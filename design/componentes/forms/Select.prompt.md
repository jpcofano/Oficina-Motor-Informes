Selector desplegable con etiqueta y nota de ayuda opcional. Base de los selectores de informe y período del panel.

```jsx
<Select label="Período" hint="Sale de la cadena de D-20." options={periodos} value={v} onChange={setV} />
```

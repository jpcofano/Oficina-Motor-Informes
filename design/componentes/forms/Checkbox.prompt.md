Fila de selección para listas de opciones binarias — el selector de secciones repetibles del panel es el caso principal.

```jsx
<Checkbox id="sec-jm" label="Uno a uno" sublabel="itera reuniones" checked onChange={setV} />
```

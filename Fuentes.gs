/**
 * Fuentes.gs — Lectura de las hojas de datos fuente.
 * Expone:
 *   leerFuente(nombreHoja, desde, hasta, filtros) -> [ {col: valor}, ... ]
 * Devuelve filas como objetos por encabezado, ya filtradas por período.
 * Se completa en: Paso 2.
 */

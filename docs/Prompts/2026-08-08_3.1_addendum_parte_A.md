# `3.1` · Addendum al prompt `2026-08-08_3_cablear_los_ecv_de_lamina_5.md` — luz verde a A, B y C

**Luz verde.** La Parte 0 midió el lote, sus valores esperados y `C.2` antes de tocar nada, que
era el punto. Se sigue con A, B y C, con los cuatro ajustes de abajo.

**El `verificador` sigue pendiente y no bloquea.** Se corre sobre el `_3` cuando la sesión
reinicie. Su reporte ahora tiene con qué contrastarse, que es mejor estreno que el original.

---

## Los cuatro ajustes

`AJ-1` · **El lote es de siete, y `ecv_barrio1-3` salen por dos razones independientes**, las dos
medidas: `opLISTA` no tiene parámetro de índice, y **no puede haber `MAPEO` para ellos** porque
no son columnas de la base sino posiciones dentro de un resultado. **Va a `PENDIENTES` como lo
que es —falta una operación que devuelva el elemento N de una lista— y no se inventa acá.**
Anotar también la consecuencia editorial: la decisión del `_18` de que salen de la misma lista
**es correcta y hoy no es ejecutable**, y son dos cosas distintas.

`AJ-2` · **⚠ `ecv_insc_dif` contradice una decisión escrita, y eso se cablea igual pero se anota
con nombre.** El `13.1` decidió el 07/08, textual: *"el 0 de `ecv_insc_dif_pct` es un dato, no un
`sin_datos`… cuando se cablee `ecv_insc_dif`, tiene que publicar cero, no un hueco"*. La medición
dice que `SUMA` va a devolver `sin_datos`. **No es un error del cableado y no se arregla en esta
corrida** —tocar `SUMA` mueve marcadores en todo el deck, no sólo acá—. Lo que va escrito es la
contradicción, con las dos puntas: la decisión del 07/08 y el comportamiento medido de `SUMA`.

**Y con el criterio que la resolvería, para que el prompt siguiente no arranque de cero:** cero
filas tras el filtro **no es lo mismo que** filas presentes con la celda vacía. La primera no
tiene nada que agregar; la segunda tiene cuatro filas que dicen que ese canal no aportó. **Es
exactamente la distinción que el `_18` ya escribió para las listas**, sin llevar a `SUMA`.
**Escribirlo como criterio candidato, no como decisión** — el prompt que lo implemente tiene que
medir primero cuántos marcadores del deck cambiarían de estado.

`AJ-3` · **`C.2` se confirma igual, por el camino del motor.** Que la aritmética contra la base
cierre en cero —`1169 + 272 + 43 + 823 + 0 = 2307`— es una buena señal y **no es la verificación**:
si el motor da otra cosa, el hallazgo es del motor y vale más que el lote entero. **Y si
`ecv_insc_dif` sale `sin_datos`, `C.2` se calcula tratándolo como cero y se dice que se lo
trató así**, en vez de reportar que no cierra.

`AJ-4` · **La corrección del conteo va escrita, porque la causa vuelve.** Nueve era mío y estaba
mal: **`FALTANTES` lista por ítem, no por token**, así que contar tokens ahí mezcla láminas —los
`@San Cristóbal (pre)` son de la lámina 6—. Es una trampa de lectura del instrumento, no un error
de suma, y va **donde `CLAUDE.md` §4 tiene las convenciones de medición**, junto a las dos que
entraron ayer. Una línea.

---

## Lo que no cambia

`A.1` y `A.2`: cada fila nace con `filtro = figura=Jorge Macri`, y el campo del filtro se verifica
en `MAPEO` antes de escribirlo.

`B.1` a `B.3`: un token por vez, no se pisa nada, no se tocan las seis filas del `_13`,
verificación dirigida contra los valores de `0.5`, **una sola corrida al final**, y la fila que dé
distinto se revierte y se sigue.

`C.1` y `C.3`: los cinco `_pct` tienen que seguir dando lo mismo que anoche, y el conteo de
`FALTANTES` contra las 264.

## Commits

Documentación aparte. Las filas de `MARCADORES` van a la bitácora con lo que midió `B.2`.

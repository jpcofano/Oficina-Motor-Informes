# `_36.1` · Addendum — una sola edición, no dos

> Addendum al `2026-08-12_36_caratula_hoy.md`, frenado en A.2/A.3. **No lo edita.**
>
> ⚠ Nota de archivo: convive con `2026-08-12_36.1_addendum_ids_de_lamina.md`, que también lleva el
> `36.1`. Los archivos no colisionan; el número sí está repetido.

---

## La edición de A.2 queda cancelada

**`ecv_barrio` se queda en la carátula (`L-052`).** La guarda del `_36` decía "nada más en la
carátula", y era para el caso de que se armara duplicando la lámina de detalle — ahí venían 31
tokens y la carátula habría publicado los mismos números que la lámina siguiente. `L-052` tiene dos
cajas, `ecv_barrio` y `enc_evento`, así que ese riesgo no existe. **La guarda estaba mal aplicada.**

## La de A.3 sí, y la hace el usuario

**`enc_evento` sale de la caja compartida de `L-035`**, que queda
`"Estrategia de comunicación:{{ecv_barrio}}"`. No por corrección: para que el evento no aparezca dos
veces seguidas. `ecv_barrio` se repite entre carátula y detalle a propósito.

Estado esperado antes de generar:

| lámina | orden | tokens |
|---|---|---|
| `L-052` carátula | 6 | `ecv_barrio` · `enc_evento` |
| `L-035` detalle | 7 | `ecv_barrio` y los demás del detalle |

---

## Parte A · Reverificar, corto

**Modelo: Sonnet.**

Repetir A.1 y A.3 del `_36`: que el bloque siga siendo `L-052` y `L-035`, órdenes 6 y 7,
**contiguas** —único requisito duro—, y que `enc_evento` ya no esté en `L-035`.

**A.2 no se repite.** Los tokens de la carátula ya no son motivo de freno.

Si el bloque no es contiguo, parar. Si `enc_evento` sigue en `L-035`, parar y decirlo: es la única
edición pendiente.

---

## Parte B · Correr y leer

**Modelo: Sonnet.**

Sin cambios respecto del `_36`: una corrida de julio, casilla de `—` tildada,
**`comunicaciones_post` afuera** por `D-27`. **Una sola, sin segundo intento.**

Del deck:

- **Seis pares carátula+detalle, no seis y seis.** Es el control de la parte.
- Cada carátula con su `ecv_barrio` y su `enc_evento`, y que **los seis `enc_evento` no sean todos
  iguales** — se esperan tres valores distintos, con los pares pre/post repitiendo por compartir
  fila de `rdv`.
- Cada detalle con su `ecv_barrio`, inscriptos y población.
- `seg_expansion` de `encuentro` contra los 36 s de `jm-20260812-110746`. **Primera lectura con dos
  láminas modelo**: va a la tabla de la bitácora.

Si cruza el corte o sale peor, `jm-20260812-110746` sigue siendo el deck y se dice en una línea.

# `_46.1` · Addendum — seguir con B y C, con una corrección que sale de la Parte A

> **Addendum al `_46`.** La Parte A ya se ejecutó y no se toca. B y C no se ejecutaron.
>
> **Modelo por parte, el declarado en el `_46`.**

**Sí: seguir de corrido con B y después C.** A.3 y A.5 se disolvieron y no hay intercambio que
decidir.

**Lo que A.2 agrega a la Parte B, y es obligatorio:** `apiBarrera1_` corre antes de que la planilla
de control esté atada, y `servirPanel_` la llama sin ninguna atadura previa. **La barrera tiene que
atar la hoja de control antes de leer `CONFIG`.** Sin eso, en contexto web app el rechazo llega como
excepción y no como rechazo distinguible — que es exactamente lo que `B.2` pide evitar. **La quinta
prueba: sin planilla atada, la barrera rechaza con motivo propio y no explota.**

La clave es `mails_autorizados`, como propone A.1. Y dejar dicho en el reporte que esto es la
**pieza 1 de `D-16`**, para que el resto de `D-16` no quede huérfano.

**En la Parte C**, después del push: correr `node tools/api.js ping` para comprobar que `/dev` sigue
andando con el `access` cambiado. Es gratis y es el único residuo que A.5 dejó abierto.

**Si el `/exec` no abre al primer intento, parar y reportar.** Hay reunión y los dos decks de hoy no
dependen de esto.

# `_35.1` · Addendum — `ULTIMO`, `enc_evento`, y la corrida que no entra

> Addendum al `2026-08-12_35_caratula_lamina4.md`, ejecutado hasta la Parte A. **No lo edita.**
> Corrige su Parte C y fija la Parte D.

---

## Las tres respuestas

**1 · `ULTIMO`, no `TEXTO`.** Corrección aceptada y es del prompt, no tuya: `opTEXTO` devuelve
`ctx.valor_fijo`, un literal, y habría publicado vacío en las seis láminas. La operación es
`ULTIMO`, la misma de `ecv_barrio`, que ya está probada leyendo del deck. **La Parte C se escribe
con `ULTIMO`.**

**2 · `enc_evento`.** Aceptado. Cae en la familia y no colisiona por prefijo.

Lo de `enc_alcance` siendo prefijo de `enc_alcance_pct` y `enc_alcance_potencial` **se anota y no
se toca**. No rompe hoy porque los tres matchean `enc_` igual; queda escrito porque el día que
alguien arme una familia más fina, muerde.

**3 · La Parte B arranca.** Y tu matiz es el correcto: no es segura porque ninguna sección tenga más
de una lámina modelo —`campana` tiene ocho—, sino porque `campana` emite cero ítems y hoy no expande
por ningún camino. El control compara las dos que sí expanden, con una lámina modelo cada una.

**Que `campana` cambie el día que tenga ítems no es un efecto colateral: es el objetivo.** Sus ocho
láminas modelo hoy salen como `[L12 ×N]…[L19 ×N]`, que no es lo que nadie quiso. Dejarlo dicho en el
commit.

---

## Parte D · Fijada

**Modelo: Sonnet.**

**Correr con el selector de `D-27` dejando `comunicaciones_post` afuera.** Recomendación tuya,
aceptada, y por el motivo que diste: sus 2 láminas resuelven cero tokens hoy, así que no se pierde
nada visible, y son 29 s.

**Una sola corrida, la de julio. Y una sola.** La proyección de ~297 s es ajustada contra un techo
efectivo de 320, y la varianza de `D-28` —316 / 204 / 220 para el mismo output— es más grande que el
margen. **Si la corrida cruza el corte o muere, no hay segundo intento**: `jm-20260811-234158`
vuelve a ser el deck que se muestra y se dice en una línea.

Del deck, leído del deck: **el orden de las láminas** —seis pares carátula+detalle, no seis y
seis— y que cada carátula traiga el `EVENTO` de la lámina que le sigue.

Y **reportar el `seg_expansion` real de `encuentro`** contra los ~144 s proyectados. La proyección
es de esta sesión y no está medida; si la real se aparta mucho, eso vale para la próxima decisión de
techo más que el deck mismo.

---

## Lo que se anota y no se toca

- **`comunicaciones_post` cambió de lámina modelo, de la 11 a la 7, sin que nadie lo declarara.** La
  lámina modelo se infiere de los tokens, así que editar la plantilla la mueve en silencio. Es un
  hallazgo de diseño, no un bug de esta corrida.
- **La lámina 11 quedó con cero tokens.**
- **La plantilla pasó de 195 a 223 tokens.**
- `enc_alcance` como prefijo de otros dos.

Los cuatro van al registro con fecha. Ninguno se arregla hoy.

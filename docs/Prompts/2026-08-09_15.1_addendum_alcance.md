# `15.1` · Addendum al `2026-08-09_15_sembrar_seccion_id.md` — se amplía a todas las columnas derivables

> Pasado por el usuario el 09/08/2026, **en texto**, no como archivo. Se transcribe literal.
> **Entra cuando cierre la Parte D del `2026-08-09_1`.** El prompt original no se edita.

---

Cuando cierre la Parte D del 2026-08-09_1, seguí con docs/Prompts/2026-08-09_15_sembrar_seccion_id.md, y ampliá el alcance: cargá todas las columnas de LAMINAS que puedas derivar de lo ya documentado, no sólo seccion_id.

El criterio de qué entra y qué no es uno solo: se siembra lo que se deriva de una fuente que sigue viva; no se siembra lo que se copia de un documento que ya es una foto.

Sí: seccion_id con las tres vías y la regla de acuerdo de dos. titulo desde la plantilla. escondida desde isSkipped() — leer está permitido, reflejar nunca decidir. Y cualquier columna de LAMINAS que hoy esté vacía y tenga una derivación estructural: decime cuáles son en la Parte 0, porque no tengo la lista de columnas final delante.

No: cobertura ni falta desde los documentos. El estado por lámina que está escrito en el handoff y en las validaciones es del 09/08 y cambia con cada cableado — sembrarlo congela una foto que va a quedar vieja el mismo día. Esas dos se derivan del motor, resolviendo los tokens de la lámina, y eso es el _14. Dejalas vacías.

Y una fila por columna en el reporte: cuántas sembradas, cuántas listadas, y por qué las listadas. Vale lo mismo que para seccion_id — llenar todo es adivinar.

---

## Dos cosas que hay que resolver antes de ejecutarlo *(medidas el 09/08, Code)*

**1 · `titulo` no es una columna de `LAMINAS`.** Las trece son `lamina_id`, `informe_id`,
`seccion_id`, `orden_plantilla`, `escondida`, `origen`, `modo`, `itera_sobre`, `filtro`, `rol`,
`cobertura`, `falta`, `notas`. Sembrar el título de la plantilla exige **agregar la columna**, que
es tocar el esquema de una hoja de registro — decisión del usuario, no de la Parte 0.

**2 · `escondida` ya está sembrada.** La escribió el sellador en la corrida del `_11`: **7 filas
marcadas** (2 de `jm`, 5 de `secco`), leídas de `isSkipped()`. No hay trabajo pendiente ahí, salvo
re-verificar que siga coincidiendo — que es lo que hace `verificarLaminas()`.

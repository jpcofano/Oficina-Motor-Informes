# `_31.2` · Addendum — la lámina 7

> Addendum al `_31` / `_31.1`. **No edita ninguno de los dos.** Inserta una parte antes de la
> Parte D y no cambia nada de lo anterior.
>
> El usuario cargó tokens en la lámina 7 de la plantilla de `jm` después del censo de A.2, así que
> **A.2 quedó vencido para esa lámina**. Antes de correr hay que mirarla.

---

## Parte E · Censo de la lámina 7 — sólo lectura, reportar

**Modelo: Sonnet, effort alto.**

Con `diagTokensDeLamina_`, el diagnóstico que ya escribiste: los tokens de la lámina 7 de la
plantilla de `jm`, uno por uno, y cuáles tienen fila en `MARCADORES`. Para los que la tienen,
`base_id · solapa · campo_logico · operacion · filtro · periodo_ref`.

Reportar tres cosas y nada más: **cuántos tokens hay**, **cuántos publicarían valor**, **cuántos
saldrían `—`**. Y si la lámina 7 es la modelo de alguna sección repetible, decirlo — cambia si sus
tokens se resuelven por ítem o una sola vez.

---

## Parte F · Cablear, con la misma regla cerrada de la Parte C

**Modelo: Opus, effort alto.**

**Se cablea sólo lo que tiene fuente que ya existe** —`MAPEO` presente, o un caso `V-*` que ya la
midió—. Lo que exija elegir fuente a ojo esta tarde **se queda en `—`**: es la misma regla que dejó
afuera a `alcance`, `clics` y los `ecv_barrio`, y no se afloja porque la lámina sea nueva.

Cada fila nueva lleva en `notas` **`SIN VALIDAR — demo 12/08`**.

**Si más de un tercio de los tokens de la lámina 7 quedan en `—`, reportar y parar.** La salida en
ese caso es esconderla, que es un clic del usuario y ya está probado.

**Sin higiene lateral.** Si al mirar la lámina 7 aparece algo de otra lámina, se anota en el reporte
y no se toca.

---

## Parte D

Sin cambios. Se corre después de F, con la lámina 7 incluida en la lectura de los dos decks.

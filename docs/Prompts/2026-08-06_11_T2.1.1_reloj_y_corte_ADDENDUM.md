# Addendum — `2026-08-06_10_T2.1.1_reloj_y_corte.md`

La Parte 0 se ejecutó y midió las cinco cosas. Este addendum fija la Parte A con esos
números y corrige lo que el prompt suponía. **El resto del prompt sigue en pie**, incluido
lo que queda fuera de alcance.

---

## 1 · La etapa 4 son dos cosas, y sólo una es atómica

La resolución (`resolverMarcadores('jm', {})`) es indivisible y cuesta ~239 s. El pintado
son ~6 s y es divisible. **El checkpoint va antes de la resolución y en ningún otro lado de
la etapa 4** — el loop de pintado no lleva ninguno: cuesta menos que la reserva y cortarlo
por la mitad dejaría tokens crudos sin ganar nada.

`costo_etapa4_seg` se llama **`costo_resolucion_etapa4_seg`** y reserva **lo indivisible**,
no la etapa entera.

---

## 2 · Las tres claves de `CONFIG`

Forma: valor en la hoja, default de módulo que nunca se lee directo, helper como único
lector — el patrón de `umbralAnclajeReunion_()`.

| clave | default | de dónde sale |
|---|---|---|
| `presupuesto_corrida_seg` | `350` | 360 duro menos el llamador de menú (~2 s) y un colchón. **No es un número medido: es el techo menos lo que se le sabe restar.** Que el reporte lo diga así |
| `reserva_cierre_seg` | `30` | cierre medido 0,8 s **+ la barrida** (~6 s reusando el mapa) + margen por la varianza observada: `tokensPorSlide_` dio 10,8 s hoy y 26,9 s a la mañana |
| `costo_resolucion_etapa4_seg` | `240` | banco del 06/08, **no una corrida**. Que el reporte lo diga |

Las tres discrepancias que va a mostrar el estado de configuración entre el `clasp push` y
el *Cargar config inicial* son ruido esperado. Anotarlo en la bitácora para que no se lea
como hallazgo.

---

## 3 · La barrida reusa el mapa de la etapa 2

`mapaTokenObjectId_` ya devolvió los mismos 195 tokens. La barrida final **no vuelve a
escanear el deck**: 6 s en vez de 17. Si por alguna razón el mapa no está disponible en ese
punto —corte antes de la etapa 2—, cae a `tokensPorSlide_` y se reporta que cayó.

La barrida corre **siempre**, haya habido corte o no: es lo que garantiza que el deck no
tenga `{{token}}` crudos. Por eso está adentro de la reserva y no antes del checkpoint.

`A.5` sigue como estaba: los tokens no alcanzados quedan `«FALTA:token»`, y la distinción
vive en el motivo de `FALTANTES`. **Es la única línea que puede cambiar por decisión del
usuario.**

---

## 4 · El corte no se persiste todavía

El `corte` viaja en el valor de retorno y nada más. **Guardarlo en la fila de `CORRIDAS` es
`T2.1.3`**, y ahí se decide si necesita columna propia. No agregar columnas en este prompt.

---

## 5 · Lo que la Parte 0 dejó dicho y hay que respetar

- **El presupuesto no arranca en `generarInforme`.** La plataforma cuenta desde `doPost` o
  desde el trigger del menú. El techo de la tabla ya lo absorbe; no volver a restarlo.
- **`FALTANTES` quedó en cero** porque el banco la pisó (`escribirFaltantes_` borra la hoja
  entera, `D-12`). Queda registrado; no es un dato de ninguna corrida.
- **Son 195 tokens, no 193.** El 193 salió de un instrumento que se había pisado a sí mismo.

---

## Lo que se espera ver

Con `presupuesto_corrida_seg` en 60 desde la hoja, sin `clasp push`:

- la corrida vuelve sola, la fila queda cerrada con conteos, `FALTANTES` lista el motivo del
  corte y el deck no tiene `{{token}}` crudos;
- **la etapa 4 no entra**, y eso es correcto: con 240 + 30 hace falta llegar a ese punto con
  menos de 80 s gastados, y las etapas 1+2 solas cuestan ~130. Que el reporte lo diga
  explícito en vez de dejarlo como sorpresa.

## El reporte

1. Dónde quedaron los tres checkpoints.
2. Qué valor quedó en cada clave de `CONFIG` y de qué medición salió.
3. Qué pasó con la etapa 4 en la prueba, y con cuántos segundos gastados se decidió no
   entrar.
4. Qué decisiones tomaste solo.
5. Qué premisa de este addendum resultó falsa, si alguna.

Sin trailer `Co-Authored-By`.

**Modelo:** Opus.

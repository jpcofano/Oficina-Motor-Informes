# `_36.1` · Addendum — las láminas se nombran por `lamina_id`, no por orden

> Addendum al `_36`, que **ya se ejecutó**. No se edita el `_36`.
>
> **Qué corrige, y no es el resultado:** la Parte A del `_36` pidió *"reportar los números"* y el
> reporte volvió en números de orden — lámina 6, lámina 7, lámina 8, la 11, 23 láminas. Bajo `D-23`
> el número **se reporta y no se guarda**: la identidad es `#lamina: L-NNN` en las notas del orador,
> y `LAMINAS.orden_plantilla` es reportado y nunca autoritativo (`CLAUDE.md` §2). El `_36` pidió mal
> y el reporte contestó a lo que se pidió; lo que queda mal es el marco de referencia, no la
> medición.
>
> **Los hallazgos A.2 y A.3 se dan por buenos** —un token de más en la carátula, `enc_evento` sin
> sacar del detalle— y la parada fue correcta. Esto los vuelve a anclar y agrega una consecuencia
> del insert que no se midió.

---

## Parte A′ · Sólo lectura — reportar y parar

**Modelo: Sonnet.**

**A′.1 — El mapa orden → ancla, de la plantilla entera.**
Correr `contarAnclasDeLaminas()` y pegar la salida de `jm` completa: `laminas`, `con_ancla` y el
arreglo `anclas` (`orden` → `ancla`), sin recortar.

Dos cosas a decir explícitamente a partir de esa salida:

- si **`L-052` existe** en `jm` y en qué orden está — el `_36` lo declara sellado como premisa, y una
  premisa no es una medición;
- si hay **alguna lámina sin ancla**.

⚠ **Ignorar el campo `veredicto`.** Esa función nació como gate de la fase de sellado y su texto
espera cero anclas; hoy que la plantilla está sellada, "GATE CERRADO" es lo esperado y no es un
hallazgo.

**A′.2 — Re-anclar A.1, A.2 y A.3 del `_36`.**
Los mismos tres resultados, sin volver a medir nada que ya se midió, escritos como
`L-NNN (orden N)`:

- qué láminas reclama `slidesModeloDe_` para `encuentro`;
- los tokens de la carátula;
- los tokens del detalle.

De acá en adelante, **todo reporte sobre láminas nombra `L-NNN` primero**. El orden va entre
paréntesis cuando ayuda a ubicarse, nunca solo.

**A′.3 — La consecuencia del insert que no se midió: `LAMINAS_CONGELADAS_`.**
`Armonizar.gs` declara para `jm` una lámina congelada **por número de slide**, con `m2_salud_camp`
de testigo. Insertar una lámina antes corre ese número, y ése es exactamente el escenario que el
testigo existe para detener.

Reportar, sin corregir nada:

1. en qué orden vive hoy `m2_salud_camp`, y qué `lamina_id` tiene esa lámina;
2. si `filtrarRenombresPorLaminasCongeladas_('jm', …)` devuelve `ok: false` hoy.

**No tocar el número ni la estructura.** Si da `ok: false`, la corrección no es cambiar un 10 por un
11 — es la misma decisión de fondo que `D-23` ya tomó para las láminas, y se decide aparte.

**Reportar y parar.** Este addendum no habilita la Parte B del `_36`: esa sigue esperando las dos
ediciones de plantilla que hace el usuario.

---

## Nota de encuadre, para el reporte — no es trabajo

El corrimiento de los 32 `post_*` **no es una tercera ocurrencia** del hallazgo de
`PENDIENTES_consistencia.md`. Ese hallazgo es que la lámina modelo se mueve **en silencio** al
editar la plantilla; acá el movimiento es la consecuencia mecánica de insertar la carátula, que se
pidió. Registrarlo como ocurrencia nueva le saca filo al patrón, que vale por los casos en que nadie
se enteró.

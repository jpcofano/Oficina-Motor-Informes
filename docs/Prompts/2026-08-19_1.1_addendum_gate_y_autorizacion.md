# 2026-08-19_1.1 — Addendum: autorización de A–E y el gate de la Parte B, corregido

> **Addendum, no edición.** El `2026-08-19_1` **se ejecutó** —su Parte 0 cerró el 19/08 con los
> nueve ítems medidos— y su cuerpo no se altera. Esto agrega lo que la medición cambió.
>
> **Las partes A, B, C, D y E quedan autorizadas**, con la corrección del gate de la Parte B que
> está abajo. **Ninguna de las dos preguntas abiertas —`camp_dir_impl` y `camp_dig_impl`— las
> bloquea:** los dos están explícitamente fuera de la tanda desde que el prompt se escribió.

---

## 1 · ⚠ El gate de la Parte B ya no sirve como está escrito

**Lo que decía:** *"Después de aplicar, correr el testigo de `0.8` otra vez. ⛔ Si la partición o
los denominadores se movieron, parar antes de la Parte D."*

**Lo que la medición del 19/08 mostró:** sobre la ventana **24–30/07, cerrada hace tres semanas**,
el denominador de `frecuencia` pasó de **475.723** (17/08) a **745.632** (19/08 18:38) — **+56,7 %**,
mientras el numerador subía **+13,4 %**. **Las cuentas de filas quedaron idénticas.**

**Entonces «el denominador se movió» no distingue nada entre días.** Un gate que dispara siempre
no es un gate: es ruido que se aprende a ignorar, que es peor que no tenerlo.

### El gate corregido

**El criterio es la partición y las cuentas de filas, no los valores.**

1. **Testigo, declarar `campo_id_cuenta`, testigo — en la MISMA sesión, minutos en el medio.**
   Fuera de eso los valores no son comparables y no se citan como si lo fueran.
2. ⛔ **Parar si cambia la partición (`4 + 22 = 26`) o alguna cuenta de filas (`4/26`, `22/26`).**
   **Eso** es lo que acusaría a la declaración: leer otras filas.
3. **Los valores se registran con hora y NO son criterio de corte.** Si se mueven, se anota; si no
   se mueven, tampoco prueba nada.

⚠ **No es una relajación: es el criterio con el que la tanda 4 cerró**, y la razón por la que cerró
así está escrita desde antes de correrla —*"la igualdad exacta puede NO darse aunque la migración
esté bien"*—. **Lo que cambió es que ahora hay número.**

### Y por qué la tanda 4 no se reabre

**Se dice acá para que nadie la reabra al leer el `+56,7 %`.** Las cuentas de filas son idénticas,
la partición cierra, y **el criterio de cierre fue la partición, no los valores — justamente porque
esto pasa.** Lo que el hallazgo sí hace es **retirar la mitad del argumento auxiliar**: el
*"denominador quieto y numerador moviéndose"* del 17/08 describía **esa tarde**, no un régimen.

---

## 2 · Dónde va cada cosa del hallazgo

**No se resuelve nada acá. Se archiva con número y fecha.**

1. **`docs/BITACORA.md`** — la entrada del día, con la tabla de las dos tomas y **la lectura de
   arriba**: por qué no reabre la tanda 4 y qué mitad del argumento retira.
2. **Addendum al caso `X-19`** (`docs/PREGUNTAS_ventana_validacion_2026-08-12_EJECUTADO.md`, que ya
   tiene el del 19/08) — ⚠ **la diferencia de −4,3 % en alcance NO se puede leer como desvío
   estable**: la base contra la que se comparó se mueve **+56,7 % en dos días** sobre una ventana
   cerrada. **El addendum del 19/08 la dejó como pregunta abierta y esto la refuerza**, no la
   contesta.
3. **`PENDIENTES`** — *"el alcance de `looker` se recalcula hacia arriba sobre ventanas cerradas"*,
   con las dos tomas. ⚠ **Un acumulado no baja y tampoco salta 56,7 % tres semanas después**: es
   recálculo de deduplicación, cambio de universo, o carga tardía. **Las tres son distintas y
   ninguna está medida. No se elige la más probable.**
4. ⚠ **`SOLAPAS.filas_datos` de `digital/CAMPAÑAS_DESGLOCE_DIGITAL` dice 4.904 y la solapa tiene
   5.037.** Es medición de `inventariarSolapas` que no se re-corrió. **Correrla** y anotar que las
   columnas de medición envejecen sin avisar.
5. ⭐ **`R-24` confirmado, y por un caso que no se buscó.** **TikTok y DV360** aparecieron en
   `DESGLOCE` y **caen en `programmatic` por la regla de resta**. Son exactamente lo que una
   enumeración habría dejado afuera. **Va a `BITACORA` junto a `R-24`**: es la mejor confirmación
   que tuvo la regla de restar en vez de enumerar, y llegó sola.

---

## 3 · `camp_alcance` — una línea más en sus `notas`

La fila que la Parte D da de alta lleva, además de lo ya pedido: **«el alcance de esta base se
movió +56,7 % en dos días sobre una ventana cerrada (19/08). Dos corridas del mismo período pueden
dar distinto y NO es un bug del motor.»**

⛔ **`camp_alcance` NO lleva `_revisar`.** La marca dice *"este número no está confirmado por
ninguna vía"* y es el caso de `camp_frecuencia` (`X-19`: el publicado no reproduce ni por ratio ni
por `looker/ALCANCE`). **`camp_alcance` reproduce, con drift** — y marcarlo también **diluiría la
marca hasta volverla decorativa**.

---

## 4 · Las dos preguntas abiertas — **quedan abiertas, y no bloquean**

### `camp_dir_impl` — ⛔ **no se cablea con este cruce**

`CONTEO` de filas de `digital/Directa Mail` reproduce en **dos de tres** campañas. **Es un buen
candidato y no alcanza**, por lo que el propio reporte declaró: **el pareo número↔lado se hizo por
orden de lectura sobre el texto del `.pptx`, sin coordenadas** — y ese es el error que el proyecto
**ya se anotó una vez**, en la sección *"Lo que no se pudo verificar"* del 12/08.

**Cablearlo ahora sería tratar un candidato como una medición**, sobre un número que **está
publicado en el deck**. Va a la **ventana de validación**, con coordenadas, como caso numerado.

⚠ **Y hay un dato del reporte que la validación tiene que mirar y que el conteo simple ignora:**
`3258` tiene **dos filas el mismo día (29/06)** y `3139` **las dos el mismo día (23/06)**. Si el
deck cuenta *envíos* y la base tiene dos filas por envío —o dos audiencias del mismo envío—,
`CONTEO` de filas **acierta por casualidad en unos casos y falla en otros**, que es justo el patrón
de «reproduce en dos de tres».

### `camp_dig_impl` — no hay definición que reproduzca

**Ni filas ni plataformas**, en ninguna de las tres. La columna **`Nomenclatura` (L)** de `DESGLOCE`
es candidata a ser el «formato», y **medirla es otro prompt** — uno que también tiene que resolver
el pareo con coordenadas, porque es el mismo cruce.

**Los dos siguen publicando `—` con motivo declarado, como el `_1` ya establece.** La lámina sale
entera y el reporte lista los dos huecos.

---

## 5 · Qué NO cambia de la Parte D

**Los nueve marcadores quedan como están escritos.** En particular:

- **`camp_frecuencia` sigue siendo el `RATIO` con `numero_revisar`.** El hallazgo lo refuerza: si
  el denominador se mueve 56,7 % en dos días, **el valor publicado va a cambiar entre corridas**, y
  los guiones son exactamente la señal de que ese número no está cerrado.
- **`ULTIMO` sobre una fila por campaña** — el gate `0.5` cerró.
- **`dimensiones` y `filtro` vacíos**, con los motivos ya escritos.

⚠ **Y un agregado a la Parte E:** los nueve valores del punto 5 se reportan **con la hora de la
corrida al lado**. Con una base que se mueve así, una tabla de números sin hora **no se puede citar
dos días después** — y el `_1` la declara insumo de la ventana de validación.

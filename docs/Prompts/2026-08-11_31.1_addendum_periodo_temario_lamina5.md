# `_31.1` · Addendum — las tres decisiones que la Parte A dejó abiertas

> Addendum al `2026-08-11_31_dos_periodos_y_dos_agregados.md`, ejecutado hasta la Parte A.
> **El prompt original no se edita.** Esto reemplaza sus Partes B y C; la D queda como está.
>
> Las tres decisiones, arriba de todo: **el segundo período es `junio_sem2`** · **el temario de
> julio suma Villa Urquiza y no suma Caballito** · **la puerta de la lámina 5 se abre, con la lista
> de qué se cablea y qué se queda en `—` cerrada de antemano.**

---

## Parte 0 · Commitear los tres diagnósticos

**Modelo: Haiku.**

`diagEncuentrosPorSemana_`, `diagLaminasEscondidas_` y `diagTokensDeLamina_` están desplegados y sin
commitear. Son sólo lectura y contestaron A.1–A.3. Commit propio, antes de tocar nada.

En el mensaje va la razón por la que uno de ellos no usa `tokensDeSlide_`: esa función devuelve `[]`
para una lámina escondida, que es correcto para la corrida y falso para un censo. Es la clase de
distinción que se pierde si no queda escrita.

---

## Parte B · Los dos períodos

**Modelo: Opus, effort alto.** Cambia qué se publica. **Supersede la Parte B del `_31`.**

**B.1 — el segundo período es `junio_sem2` (12–18/06).** Aceptada la recomendación de A.1, y por el
motivo que la Parte D va a mostrar: **barrios disjuntos**. Boedo, Almagro y Mataderos contra los de
julio, sin un solo solapamiento. Que además ya exista en `PERIODOS` y ya haya generado un deck es
lo que hace barata la decisión, no lo que la justifica.

Queda **una sola alta**: el período de julio, 24–30/07, corte viernes–jueves (`R-11`), con `notas`
diciendo que es de la demo.

**B.2 — el temario de julio, y acá está la decisión que A.4 marcó bien.**

A.4 detectó la tensión y tiene razón en no resolverla sola. Se resuelve así, y el criterio no es
la ventana:

- **San Cristóbal se queda**, con fecha 23/07 y en el período de julio. Cae fuera de la ventana y
  no importa: `R-17` y `R-21` dicen que **el temario selecciona y los filtros acotan**, y su fila
  de `rdv` existe y ya se ancló bien —138 inscriptos en el deck de anoche, que es el mismo número
  que `V-71` mide en el deck publicado—.
- **Se agrega Villa Urquiza** al temario de julio. `V-71` mide que el deck que el equipo publicó esa
  semana tiene cuatro encuentros individuales: Retiro 98, Villa Urquiza 1344, Belgrano 753 (que es
  Orden Público) y San Cristóbal 138. Con Villa Urquiza cargada, el temario de julio **es
  exactamente ese conjunto**.
- **Caballito no se agrega**, aunque esté en `rdv` dentro de la ventana. El deck publicado no lo
  trae: el temario cura, y ésa es justamente la propiedad que `R-17` describe. Agregarlo para que
  "cierre con `rdv`" sería confundir el temario con un recorte por fecha.

Cargar Villa Urquiza con `cargarTemario(texto, 'periodo_de_julio')`, mirando primero qué formato
parsea `parsearLineaReunion_`. **Nace con `mostrar` vacío** y hay que ponerla en `sí`, explícito en
el reporte.

Backfill de `periodo_id` al período de julio en las 7 filas que ya existen. Las dos de
`tipo = Agregado` también: `anclarEncuentros` ya las excluye por su cuenta, pero una fila sin
período no entra a ningún informe (`D-19`) y dejarlas vacías las convierte en un pendiente invisible.

**B.3 — el temario de `junio_sem2`.** Los cuatro que midió A.1: Boedo 158, Almagro 498, Boedo 210 y
Mataderos 286. Los dos Boedo son encuentros distintos con fechas distintas — cargar los dos, y si
la clave del anclaje no los distingue, **reportarlo y parar**, que eso sí es un hallazgo y no un
trámite.

**B.4 — el filtro.** `leerReuniones_` filtra además por `periodo_id`. Las excluidas se reportan con
motivo, como ya hace la rama `CAMPANAS` citando `D-19`. Una fila sin `periodo_id` no se asigna a la
semana vigente: se lista.

**Control de la parte:** julio tiene que emitir **6 ítems** —los 5 de siempre más Villa Urquiza— y
`junio_sem2` **4**. Otro número, se para.

---

## Parte C · La lámina 5

**Modelo: Opus, effort alto.** **Supersede la Parte C del `_31`, cuya puerta A.2 midió cerrada de
entrada (38,7 %).**

**La puerta se abre, y la lista está cerrada de antemano para que no se decida sobre la marcha.**

**Se cablean seis**, y sólo seis, porque los seis tienen fuente que ya existe y no hay que elegirla:

- `cc_base`, `cc_contactados`, `cc_contact_pct` — `looker/CC x Cuentas`. Ya validados: `V-64` 6011,
  `V-65` 1878, `V-66` 31 %.
- `ivr_75`, `ivr_75_pct`, `ivr_marque1` — `digital/Directa IVR`, con el `MAPEO` que ya está:
  `ivr_e75`, `ivr_e75_pct`, `ivr_marque1`.

**Se quedan en `—`, por decisión y no por falta de tiempo:**

- `alcance` y `clics` — nombres sin prefijo y sin fuente evidente. Elegirles una fuente esta tarde
  es enumerar a ojo, que es el modo de falla de `§5.1`. Publican `—`, que es honesto.
- `ecv_barrio1/2/3` — la lámina ya trae `ecv_barrios`, que es la lista `LISTA` con el catálogo.
  Los tres slots son redundantes y llenarlos pediría una operación de posición que el motor no
  tiene. **No se inventa la operación**, igual que con `m2_campanias`.
- `ecv_insc_dif` resuelve `sin_datos` y así se queda: **sin dato no es cero** y ya está medido.

**El universo del agregado son los encuentros que el temario seleccionó para ese `periodo_id`**, no
un recorte por fecha sobre `rdv`. Después de la Parte B ese conjunto está bien definido, y es lo que
hace que el agregado y las individuales coincidan por construcción y no por haber empatado un
número.

Cada fila nueva lleva en `notas` **`SIN VALIDAR — demo 12/08`**.

**Recuento al cerrar:** de 31 tokens quedarían **5 en `—`** — `alcance`, `clics` y los tres
`ecv_barrio` — más `ecv_insc_dif` en sin dato. Si el recuento real da más de 8, **reportar antes de
la Parte D** y no seguir: ahí la decisión vuelve a ser esconderla, que es un clic y ya está probado.

---

## Parte D · Dos corridas, dos decks

Sin cambios respecto del `_31`, con los números de control actualizados: **julio 6 encuentros,
`junio_sem2` 4**, y las dos listas de barrios disjuntas entre decks.

# Addendum · 2026-08-22 · Paso `2026-08-22_23` — después de la Parte A

**Fecha:** 2026-08-22, con la Parte A corrida y sin commitear.
**Addendum a:** `docs/Prompts/2026-08-22_23_dos_fases.md` — **que no se edita.**

Las Partes B y C siguen pendientes y corren como están, **con estos cinco agregados**. Cuatro
salen de preguntas que el reporte dejó bien planteadas.

---

## 1 · Sí, el encuadre lleva `D-NN` · **afecta la Parte A**

La pregunta del reporte es correcta y `CLAUDE.md` §7 la contesta: **una decisión estructural va a
§1 con ID estable**. Un encuadre sin ID *"no se puede citar ni derogar"* — y éste va a citarse cada
vez que alguien pregunte por qué algo no va ahora.

**Darle su `D-NN`** —número libre de la serie, sin reusar— y dejar el texto de §2 como puntero,
no como copia. Reportar cuál tomó.

---

## 2 · Las fases se llaman por su nombre, no por su número · **afecta la Parte A**

⛔ **La colisión que el reporte encontró es real y se resuelve renombrando, no señalando.**
`PLAN.md` ya usa *"Fase 1 / Fase 2 / Fase 3"* para el sellado de láminas de `D-23`. Dejar dos
juegos de "Fase N" conviviendo con una nota al lado es pedirle al lector que recuerde cuál es cuál.

**Las dos nuevas se llaman `informe semanal` e `informe actualizable`** — son las palabras que usó
el usuario y no colisionan con nada. El número puede acompañar, nunca ir solo.

---

## 3 · ⛔ La condición 3 está medida contra la corrida equivocada

La propuesta pone la condición 3 —*cada número en su lámina*— en ⛔ con *"tres copias con tres
juegos de cifras"*. **Esa evidencia sale de `jm-20260821-230048`**, la desatendida que corrió con
`R-11 (calculado)` y trajo doce encuentros de junio y julio. **No es el testigo.**

⚠ **Y el usuario ya revisó el testigo `jm-20260821-234927` y dio la lámina por buena:** la portada
dice *"Encuentro Temático Salud - Eje Sur"* y el iceberg dice *"Parque Patricios"* porque **uno es
el nombre y el otro el barrio**. No es una portada cruzada.

**Volver a evaluar la condición 3 contra `234927` y sólo contra `234927`**, y reportar el estado
que corresponda. Si el defecto no se reproduce ahí, la condición cambia de color y hay que decir
que la observación original era de otra corrida.

⛔ **Es la tercera vez que se concluye contra `230048`.** Anotar en `PENDIENTES` la regla que lo
evita: **`jm-20260821-234927` es el testigo de fase `informe semanal`** —período elegido, temario
correcto, sin corte— y ninguna conclusión sobre el producto se saca de otra corrida sin decir por
qué. Que un deck exista no lo hace comparable.

---

## 4 · La condición 6 destraba el prompt de los estados · **afecta la Parte B**

El reporte tiene razón: si la condición 6 se aprueba —*cada número publicado con caso en estado
terminal*—, entonces **`2026-08-21_20_estados_del_csv` deja de "no bloquear nada"**. No se puede
exigir un estado terminal cuando el vocabulario de estados no está declarado en ningún lado.

Tres cosas, y ninguna reordena frentes:

1. ⛔ **Ese prompt nunca se commiteó.** Existe escrito y no está en `docs/Prompts/`. **Copialo**,
   con su nombre, en la Parte B junto al `_24`.
2. **Sacarle el ⏸ y la nota *"no bloquea nada"***, que quedó vencida. Va con addendum fechado en el
   propio archivo, no editando su cuerpo.
3. ⚠ **Su `0.1` espera diez estados y el CSV tiene once** —entró `pendiente` con el `_18`—. Eso no
   lo invalida: su `0.1` dice explícitamente *"no partir de ninguna lista previa"*. **Anotarlo**
   para que quien lo corra no lo lea como una contradicción.

---

## 5 · La convención existe: el arreglo es una línea, no un pendiente · **afecta la Parte C**

El reporte midió lo que la Parte C preguntaba, y la respuesta cambia qué hay que hacer:
`CLAUDE.md` §3 **sí** tiene la convención —*"`AAAA-MM-DD_N_descripcion.md`, con la fecha real del
día… y un número de orden dentro del día"*—.

⭐ **El hallazgo no es que falte: es que *"dentro del día"* no dice *"reinicia con la fecha"*, y el
incumplimiento no se ve leyendo la regla.** Una convención que se puede cumplir mal leyéndola bien
es una convención mal escrita.

**Precisar esa línea en `CLAUDE.md` §3** —que el número reinicia con la fecha, con las tres
colisiones como testigo— y que `PENDIENTES` sólo registre las colisiones, no la regla. Es donde §7
la pone y donde alguien la va a leer justo antes de repetir el error.

**Lo demás de la Parte C no cambia:** no se renumera nada ejecutado.

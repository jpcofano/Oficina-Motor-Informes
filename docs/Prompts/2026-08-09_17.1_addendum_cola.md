# `17.1` · Addendum a la corrida nocturna del `09/08` — lo que queda, en orden

> El `_17` se ejecutó parcialmente: `N1` cerró su Parte A más `R-22` y el `hoja_default`; `N2` y
> `N3` no arrancaron. El prompt original **no se edita**. Esto es la cola corregida.

---

## 1 · Lo que decidió la nocturna y queda firme

Nada de esto se reabre:

- **El camino del seed para `SOLAPAS`, y no la celda a mano.** Las catorce filas afectadas tenían
  `origen = seed` y `aplicarClasificacionSolapas_` pisa toda fila seed: escribir a mano habría
  dado ping-pong con el sembrador en cada corrida. El precedente está en `Instalar.gs`, en el
  comentario que retiró `reclasificarSolapasM2Invertidas_` por exactamente eso. **Decisión
  correcta y bien fundada.**
- **`looker/resumen_metricas` queda en `derivada`, no baja a `ignorar`**, aunque el `_1` la
  liste. `derivada` ya la corta en `buscarMapeo` y además **dice qué es**. Bajarla no cambia
  comportamiento y borra información. **Se acepta y va a `PLAN.md`** — es la clase de decisión que
  alguien va a querer revertir dentro de dos meses sin saber por qué se tomó.
- **Los tres números de `N1` quedan asentados en `BITACORA.md` con fecha y hora de lectura**, si
  no quedaron ya: `979/979`, la acotación de dimensión de `looker/Cuentas`, y el corrimiento del
  resumen de `jm` a `sin_datos 7 / error 6`. **El 979 hay que anotarlo como predicción acertada**,
  no sólo como medición: se predijo antes de mover el default y dio al dígito. Eso vale más que
  el número.
- **La premisa que se cayó va escrita donde estaba escrita mal:** `instalar()` no toca `SOLAPAS`
  ni el contenido de `BASES` — van por `sembrarClasificacionSolapas` y `aplicarSeedConfiguracion_`.
  Costó dos intentos y va a costárselos a cualquiera que venga después. Si no está en
  `RUNBOOK.md`, ponerla.

---

## 2 · La cola

**El orden es el mismo, con un archivo nuevo adelante.**

| | qué | nota |
|---|---|---|
| **`N1`** | `2026-08-09_1`, Partes **B, C y D** | **La Parte D se lee en `1.4`, no en el original** |
| **`N2`** | `2026-08-09_16` — la columna `titulo` | Corregido el 10/08 en el lugar; sigue sin ejecutarse |
| **`N3`** | `2026-08-09_15` + `15.1` — `seccion_id` | Sin cambios |

**`N2` antes que `N3` sigue siendo firme:** `N2` cambia el esquema de la hoja que `N3` escribe.

**Las prohibiciones del `_17` siguen vigentes tal cual** — ninguna plantilla se escribe, ningún
número se valida contra un informe publicado, backup antes de escribir en la planilla.

---

## 3 · La regla de contexto, que se ejerció bien y por eso se escribe

`N1` se detuvo por margen de contexto y reportó, en vez de entregar `N2` y `N3` a medias sobre una
hoja de registro recién creada. **Fue la decisión correcta y queda como regla de las corridas
nocturnas:**

> Cuando el margen no alcanza para terminar una tarea, **no se empieza**. Se reporta dónde quedó
> la cola. Una tarea trabada con su motivo vale más que tres a medias — y sobre una hoja de
> registro, una tarea a medias deja un estado intermedio que hay que limpiar antes de poder
> seguir.

El reporte de esta corrida pudo cerrar con *«lo que no arrancó, no arrancó, no hay estado
intermedio que limpiar»*. Ésa es la frase que la regla busca poder escribir siempre.

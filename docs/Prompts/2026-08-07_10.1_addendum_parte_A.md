# `10.1` · Addendum al prompt `2026-08-07_10_prioridad_de_seleccion.md` — luz verde a la Parte A

**Luz verde.** La Parte 0 corrió y reportó. `0.5` cierra el riesgo de alcance: ninguna corrida
ejecutó la versión perdedora sobre `campana` y **el código ya implementa la ganadora**. Se
sigue con la Parte A, con los seis ajustes de abajo. El prompt original **no se edita**.

**Cero `.gs`. Cero hojas. Cero corridas.** Sigue siendo documentación.

---

## Los seis ajustes

`AJ-1` · **El dueño queda resuelto: `R-17` en `docs/REGLAS_NEGOCIO.md`.** Donde `A.1` dice *"el
dueño que confirme `0.3`"*, se lee `R-17`. **No** se abre una `D-NN` y **no se toca `D-09`**:
el régimen se sigue declarando por sección, y la prioridad entre regímenes no cambia dónde vive
esa declaración.

`AJ-2` · **`R-16` no se corrige al pie: recibe un addendum fechado y `R-17` la supersede
parcialmente.** `0.1` encontró la versión perdedora **en el título**, así que no hay línea al
final que la arregle. La división es exacta y hay que escribirla en las dos puntas:

| de `R-16` | qué pasa |
|---|---|
| el criterio de solape, y su motivo (las campañas empiezan unos tres días antes) | **queda vivo**, está medido — IVR dejó de dar cero |
| *"y el default es la semana"* del título, y el párrafo que lo desarrolla | **superseded por `R-17`** |
| la cláusula de filtros *"sobre los días activos dentro de la semana"* | **superseded por `R-17`** — ver `AJ-3` |
| el bloque `⚠ CONTRADICCIÓN ABIERTA` | se reemplaza por el puntero a `R-17`, con fecha |

El addendum de `R-16` va **fechado 07/08/2026**, dice qué partes caen y cuál queda, y apunta a
`R-17`. `R-17` dice, del otro lado, qué parte de `R-16` supersede y cuál **no**.

`AJ-3` · **La cláusula de filtros de `R-16` no la anticipaba el prompt, y es el hallazgo que
más cambia.** Acota los filtros del nivel 2 a *los días activos dentro de la semana* — la
ventana entrando por la puerta de atrás. Con la decisión del 07/08 los filtros acotan **lo que
el temario ya eligió**, y el temario busca **en toda la base**. Escribirlo así en `R-17`, y
declarar en el addendum de `R-16` que esa cláusula queda superseded. Es un punto donde un
lector podría creer que sigue habiendo filtro de ventana.

`AJ-4` · **Los tres filtros no se escriben: se apunta a `R-15`.** El segundo bullet de `A.1`
pedía nombrarlos con su valor exacto porque `0.4` los daba por probablemente no escritos. Están
desde el 04/08, medidos y re-verificados. `R-17` **cita `R-15`** y no repite los valores: una
sola fuente de verdad, y un valor duplicado es un valor que se desincroniza.

`AJ-5` · **`R-17` describe lo que el motor ya hace, no un cambio pendiente.** `0.5` midió que
`itemsDeSeccion_` filtra `CAMPANAS` por `informe_id`, `mostrar` y `periodo_id` no vacío
(`D-19`) más `SECCIONES.filtro`, y que las **dos** llamadas al solape que existen están del
lado de los agregados. Escribir esa correspondencia en `R-17` **como estado verificado con
fecha**, nombrando las funciones y **sin citar líneas** — envejecen. Es lo que convierte a
`R-17` en descripción y no en deuda.

`AJ-6` · **`A.3` se ejecuta como está**, con dos precisiones: la `[?]` de la contradicción se
cierra apuntando a `R-17`, y `CONFIG_INFORMES.md` §1.1 **pasa a apuntar**: conserva la decisión
editorial y el caso testigo, y deja de ser el lugar donde se lee la regla general. La `[?]` de
la campaña que cruza dos semanas **no se toca**.

---

## Una sola fuente de verdad — quién es dueño de qué

Después de esta corrida, cada pregunta tiene **un** documento donde se lee la respuesta. Los
demás **apuntan** y no repiten el texto.

| pregunta | dueño | los otros |
|---|---|---|
| ¿Qué régimen decide qué campañas entran, y en qué orden de prioridad? | **`R-17`** | `R-16` y `CONFIG_INFORMES.md` §1.1 apuntan |
| ¿Cómo se decide si una fila toca la ventana? (el solape, y los agregados) | **`R-16`** | `R-17` apunta y **no lo reescribe** |
| ¿Cuáles son los filtros JM / GCBA, canal por canal, con su valor? | **`R-15`** | `R-17` apunta y **no copia los valores** |
| ¿Qué decidió el equipo para `jm` sobre campañas destacadas, con su caso testigo? | **`CONFIG_INFORMES.md` §1.1** | `R-17` cita el caso testigo por referencia |
| ¿Qué versión quedó derogada y dónde vivía? | **`R-17`** | `A.1` del `_4` **no se edita**: se lo cita |

**El addendum de `R-16` es un puntero fechado, no una copia.** Dice qué partes de `R-16` deja
de regir y a dónde ir a leerlas. Si el addendum termina explicando la prioridad de tres
niveles, se rompió la regla: eso vive en `R-17` y sólo ahí.

---

## Lo que `R-17` tiene que llevar

- **Los tres niveles en orden**, con el tercero dicho como **fallback y no como filtro
  previo**: la semana decide **sólo cuando no hay temario**. Es la frase que faltaba.
- **El nivel 2 apuntando a `R-15`** (`AJ-4`).
- **El caso testigo**: San Cristóbal 23/07 entra con ventana 24–30/07 (`CONFIG_INFORMES.md`
  §1.7).
- **Qué versión queda derogada y dónde vivía**: `A.1` del prompt `2026-08-07_4_once_respuestas.md`,
  **citado y no editado** — vive en un prompt ejecutado.
- **Qué NO cambia**: la ventana sigue rigiendo los agregados (`ecv_*`, ministros, `m2`), y el
  criterio de solape de `R-16` sigue vigente ahí.
- **Cómo se verifica**: hoy no se puede sobre `campana` — `CAMPANAS` no tiene filas de `jm`.
  La verificación queda declarada para cuando las haya, y **eso se dice**, en vez de darla por
  hecha.

---

## Commits

Uno por archivo tocado. Documentación. Sin `—`. `git push` después de cada uno.

## Verificación

Un `grep` por la versión perdedora devuelve **sólo** citas marcadas como derogadas o
superseded, y las dos puntas —`R-16` y `R-17`— dicen lo mismo sobre qué quedó vivo.

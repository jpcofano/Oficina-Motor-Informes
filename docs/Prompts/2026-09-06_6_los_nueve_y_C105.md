# 2026-09-06_6 — Los nueve recuperados, y `C-105` como `exacto`

**Cuatro partes.** Las dos primeras mueven números del deck; las dos últimas cierran huecos de
método.

⚠ **Insumo que no está en el repo:** el log de `diagCorteAgenda()` corrido **después** de que el
usuario pasara la celda de `MAPEO` a `D`. ⛔ **Si el usuario no lo adjuntó, la Parte B no corre**:
se anota y se sigue con las demás.

> ⭐ **NOTA DE CODE:** el log **sí llegó**, con la corrida de las **14:40 del 07/09**. Está
> transcripto en la Parte B.

---

## Reglas

1. ⛔ **No corras nada contra la planilla viva.** ⛔ **No hagas `clasp push`.** ⛔ **No toques
   plantillas.**
2. ⛔ **Si algo necesita una decisión no escrita, PARÁ ESA PARTE** con la pregunta exacta.
3. **Un commit por parte.** ⛔ **Ningún número al reporte sin el comando que lo produjo.**
4. ⭐ **El cruce se hace sobre el BLOQUE entero, no sobre los casos que este prompt nombra.**

**Subagentes:** `verificador` al cerrar `A` y `B`. ⛔ `cableador` **no se usa**.

---

## Parte 0 — Premisas · **Sonnet** · effort normal · SÓLO LECTURA

| # | premisa | reproductor | esperado |
|---|---|---|---|
| **0.1** | Los nueve recuperados **existen** como marcadores | `for m in camp_clics camp_entregados camp_impresiones camp_visualizaciones camp_google_clics camp_meta_clics camp_meta_impresiones camp_meta_vistas camp_prog_vistas; do cut -f1 docs/_snapshots/MARCADORES_2026-08-31.tsv \| grep -cx "$m"; done` | **1 cada uno** ⚠ El snapshot es del 31/08: prueba que el nombre existe, **no** qué formato tiene hoy |
| **0.2** | Los dos fabricados **ya no están** | mismo comando con `camp_google` y `camp_meta` | **0** los dos |
| **0.3** | La lista de hoy son ocho | leer `GUIONES_A_LEVANTAR_` | los 4 del `C-99` + 3 `emin_*` + `imp_prog` |
| **0.4** | `C-105` está en estado `cerrado` | leer `C-105` entero | `cerrado`, y por eso `D-60` no lo levanta |
| **0.5** | El rastro que escribe la sobreviviente es sólo la fecha | leer la escritura de `notas` en `guionesValidados_` | no nombra el caso |

---

## Parte A — Los nueve entran, si su `exacto` habla del valor · **Opus** · effort alto

**Decisión del usuario, 06/09:** ⭐ *«si hay un `exacto`, se valida, ya está. Los nuevos que
contradicen son por movimiento de bases»*. ⇒ **Los nueve entran a `GUIONES_A_LEVANTAR_`.**

### ⛔ Pero antes, la verificación que `C-102` enseñó a hacer

**Para cada uno de los nueve, leé el caso que lo declara `exacto` y decidí qué afirma:**

| si el caso afirma | entonces |
|---|---|
| **el valor** —un número comparado contra algo— | ⭐ entra |
| **el formato** —un signo, un separador, un redondeo— | ⛔ **no entra**, y se dice por qué |

⚠ **`C-102` valida el `%` duplicado, no el universo**, y estuvo a punto de habilitar tres
levantamientos que no correspondían. **La pregunta no es «¿hay un `exacto`?» sino «¿sobre qué?».**

⭐ **Y esto no es reabrir la decisión del usuario:** él decidió que **un `exacto` alcanza**. Lo que se
está midiendo es **si el caso es un `exacto` del número** o de otra cosa. Son preguntas distintas.

- ⛔ **Verificá los nueve nombres contra `MARCADORES` VIVO**, no contra el snapshot del 31/08 —que es
  anterior a la migración a `'*'`—. Si alguno no está en vivo, **fuera de la lista y reportado**.
- ⭐ **Reportá los nueve por identidad**, cada uno con su caso y con qué afirma ese caso. ⛔ **Un
  «entran nueve» sin esa columna no es auditable.**
- La lista sigue **explícita y congelada con su fecha**. ⛔ **Levantar son DOS escrituras**;
  el **gate de identidad de ministros se mantiene** y aborta la operación entera si no pasa.

⛔ **No la corras.**

`verificador` al cerrar. Un commit.

---

## Parte B — `C-105` pasa a `exacto`, con evidencia · **Opus** · effort alto

⭐ **Sí: corresponde `exacto`.** El criterio de `ministros` lo fijó el usuario —*«la lista sale de la
ventana y listo»*— y el corte por `D` lo implementa. `cerrado` fue la elección conservadora cuando
todavía no había corrida; **ahora la hay**.

⛔ **Pero `C-105` no se edita.** Es evidencia fechada. **Se escribe un caso nuevo que lo supersede**,
en estado `exacto`, sobre `emin_lista` y `emin_encuentros` — la forma de `D-58`.

**Su evidencia es el log de `diagCorteAgenda()` adjunto**, y el caso tiene que citar **los dos
nombres que deciden**: ⭐ **Sabor entra, Quirós no.** ⛔ **No cites el conteo** —6 contra 7—: es lo que
dio verde sobre las siete equivocadas.

- ⛔ **Si el log no está adjunto, o si dice que Quirós sigue entrando, NO escribas el caso.** Anotá
  que la Parte B queda pendiente de esa corrida y seguí. **Un caso `exacto` sin la corrida que lo
  respalda es exactamente lo que `D-60` no cubre.**
- Con el caso escrito, **`emin_lista` y `emin_encuentros` entran a la lista de la Parte A.**

`verificador` al cerrar. Un commit.

---

## Parte C — El rastro que se perdió al retirar la vieja · **Sonnet** · effort normal

`levantarRevisar_` escribía en `notas` **qué caso validó** —*«Validado por `V-113` (identidad
interna)»*—. La sobreviviente escribe **sólo la fecha**.

⭐ **La fecha dice cuándo; el caso dice por qué.** Sin el caso, dentro de un mes la celda no permite
reconstruir en qué se apoyó el levantamiento, y hay que ir a buscarlo a un log.

⇒ Que `guionesValidados_` escriba **el caso y su fecha**. ⛔ **Sin romper la defensa de las dos
escrituras**: lo que se agrega es contenido de `notas`, no una tercera operación.

Banco: la celda resultante nombra el caso. **Control por identidad, no por longitud.**

Un commit.

---

## Parte D — El incidente del desarme, registrado como método · **Sonnet** · effort normal

⛔⛔ **El arreglo del desarme pasó de subcontar a FABRICAR NOMBRES.** `V-111` tiene dos grupos de
llaves y la expansión no hacía el producto cruzado ⇒ `camp_google` y `camp_meta` entraron **con
`exacto` vigente y no existen**.

⭐⭐ **Y es peor que subcontar**, por el motivo que hay que dejar escrito: un nombre inventado con
`exacto` vigente **entra a la lista de levantamiento** — o sea, el error viaja hasta una escritura en
la hoja. Subcontar sólo esconde.

⭐⭐ **Y el control sintético no lo cazó: probaba tres formas y la real tenía una cuarta.** Lo destapó
cruzar contra el snapshot.

⇒ **Lo que se registra como método, no como anécdota:**

- **Un control sintético prueba lo que su autor imaginó.** ⭐ **Cruzar contra un padrón real es parte
  del método, no un extra** — es lo único que encuentra la forma que nadie previó.
- **Todo nombre derivado de un desarme se verifica contra `MARCADORES` antes de usarse.** Un nombre
  que no existe **no es un hallazgo: es un defecto del que lo produjo.**
- El control del desarme **ganó el caso de doble llave** — dejalo dicho, con la fecha.

Va donde `CLAUDE.md` §7 mande. ⛔ **Grepeá antes: si la lección ya está, registrá el cero** y sumá
sólo lo nuevo.

Un commit.

---

## El reporte

1. La tabla de la Parte 0.
2. Una fila por parte: parte · estado · commit · una línea.
3. **«Para correr»**, en orden, con **qué tiene que dar cada una**.
4. **«Bloqueado, decide el usuario»** — la pregunta exacta, una línea.
5. Lo que encontraste y no estaba en el prompt.

## Orden

```
0 → A (Opus)   ← si sólo entra una, es ésta
    B (Opus)   ← depende del log adjunto
    C (Sonnet)
    D (Sonnet) ← ⚠ la primera que se cae
```

# ADDENDUM 4 al `2026-09-11_4` — dos huecos de A.bis, antes de correrla

⛔ No correr `aplicarRenombreAcumMail` hasta cerrar estos dos. El diseño está bien; le faltan
dos piezas y las dos son mecánicas.

## 1 ⛔⛔ El renombre NO agrega nada al seed — y ése era el motivo de la decisión

El comentario del código dice *«se renombran Y SE AGREGAN AL SEED»*, pero `aplicarRenombreAcumMail`
sólo toca la hoja. ⇒ **`SEED_MAPEO_ACUMULADO_` sigue teniendo 3 filas de `Mail`**
—`acm_id_cuenta`, `fecha_periodo`, `acm_remitente`—, y las otras **9 quedan igual de huérfanas
que antes**, sólo que ahora con nombre `acm_*`, **que sugiere falsamente que vienen del seed**.

⭐ Eso es peor que el estado actual: hoy el prefijo `mail_*` delata que son de otra solapa; después
del renombre nada las distingue de las sembradas.

⇒ **Las 9 se agregan a `SEED_MAPEO_ACUMULADO_` en el mismo commit**, con letra, encabezado y nota.
⛔ **Verificación:** correr el sembrador después y que `agregadas` o `sin cambios` diga que ya
estaban — ⚠ pero **no creerle al resumen**: acaba de informar *«agregadas: 0 · sin cambios: sí»*
después de agregar siete filas. **Contar por el export directo.**

## 2 ⛔⛔ `aplicarMudanzaMail()` deshace el renombre si alguien la vuelve a correr

Quedó en el código con `MAPEO_ACUM_MAIL_` y sus 12 nombres `mail_*`. Una segunda corrida
**re-crea las 12 filas viejas** y vuelve a dejar dos columnas con dos nombres — sin fallar, y sin
que nada avise.

⇒ **`MAPEO_ACUM_MAIL_` se actualiza a los nombres finales en el mismo acto**, o la función se
neutraliza con un gate que aborte si la mudanza ya está hecha. ⭐ Preferible lo primero: una
función que se puede correr dos veces sin romper es mejor que una que hay que recordar no correr.

## 3 ⚠ Dos cosas del plan que están bien y conviene no perder

⭐ **`acm_remitente_dir` (G) contra `acm_remitente` (AI)** es la distinción correcta: dirección
cruda contra etiqueta normalizada. `jmacri@` 424 + `jorge.macri@` 384 = 808 = el conteo de `JM`.

⭐ **El gate que exige que la fila del seed exista antes de borrar la duplicada** evita dejar dos
columnas sin mapeo. Y **actualizar los marcadores antes de borrar** es el orden correcto.

⚠ **Y el split por `/` en `camp_or`/`camp_ctor`**: bien visto — reemplazar el string entero habría
dejado la fórmula a medias publicando `FALTA`.

## Después: la Parte B otra vez

⛔ Los siete tienen que seguir dando `444.403 · 447.145 · 150.506 · 1.376 · 3 · 33,9 % · 0,9 %`.
Un renombre a medias **no falla**: publica `FALTA`. ⇒ y recién ahí la Parte C.

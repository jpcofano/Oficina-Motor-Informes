# 2026-09-11_1 — La corrida de `jm`

⛔ Opus, effort alto. Genera un deck.
⛔ `L-010` NO entra: lo está revisando el usuario en la plantilla. No lo diagnostiques.

`D-57`: se dispara y se lee de `CORRIDAS`. Invariante: **`corte ⇒ pendientes ≥ 1`**.

**1 · La pregunta 4, que quedó sin contestar:** `cc_campanias` publica **entre guiones**. Es de
`jm` y tiene 0 apariciones en `secco`, por eso la corrida de ayer no pudo decir nada.

**2 · El par de implementaciones de `jm`**, que el usuario descruzó ayer: cada valor en su
casillero. Con el lector de coordenadas, no con el texto aplanado.

**3 · Censo del deck de `jm`:** cuántos `/////`, en qué láminas, y cuántos crudos.
⛔ **Censar y reportar. No abrir casos, no proponer cableado, no escribir ninguna fila.**

⚠ `jm` nunca tuvo este recorrido, así que va a salir mucho. **Eso es un inventario, no una
cola de trabajo** — la cola la arma el usuario después.

# 2026-09-11_1 — La corrida de `jm`

⛔ Opus, effort alto. Genera un deck.

⛔⛔ `L-010` de `secco` NO se toca y NO se valida en este prompt. El usuario corrigió TODOS sus
tokens el 11/09 —estaban todos mal, heredados de `jm`— y sacó `post_formato1-4`, que en `secco`
no existen. ⛔ No abrir casos sobre esa lámina, no proponerle controles, no diagnosticarla.
Se registra la decisión y se sigue.

## Lo único que se escribe de `L-010`

⭐ **Decisión del usuario, 11/09/2026: el FORMATO no se calcula, porque no se sabe cómo.**
⛔ Eso no es «falta cablearlo» ni «lo escribe una persona»: es **sin regla de cálculo conocida**,
una causa distinta. No recibe fila, no entra a `C.1`, y **no vuelve a la cola de cableado** hasta
que alguien defina la regla. Va a `CONFIG_INFORMES` §4.4, una línea, al lado de las otras causas.

## La corrida

`D-57`: se dispara y se lee de `CORRIDAS`. Invariante: **`corte ⇒ pendientes ≥ 1`**.

1. **`cc_campanias` publica entre guiones** — la pregunta 4 que quedó abierta ayer. Es de `jm` y
   tiene 0 apariciones en `secco`, por eso la corrida anterior no pudo decir nada.
2. **El par de implementaciones de `jm`**, que el usuario descruzó: cada valor en su casillero.
   Con el lector de coordenadas, no con el texto aplanado.
3. **Censo del deck de `jm`:** cuántos `/////`, en qué láminas, cuántos crudos.

⛔ **Censar y reportar. No abrir casos, no proponer cableado, no escribir ninguna fila.**
⚠ `jm` nunca tuvo este recorrido: va a salir mucho. **Eso es un inventario, no una cola de
trabajo** — la cola la arma el usuario.

---

## ⚠ Nota de ejecución — 11/09/2026

**La corrida de la sección «La corrida» se ejecutó con la versión anterior de este prompt**, que
era idéntica en esos tres puntos: `jm-20260911-120257`. Lo que esta versión agrega es el bloque de
`L-010`, que es de `secco` y **no afecta a `jm`** — `JM_marcada` no se modificó desde el 10/09
22:43:11, o sea antes de esa corrida. **No se volvió a correr**: habría dado las mismas respuestas
con los números movidos por el drift de la fuente, y eso no agrega evidencia.

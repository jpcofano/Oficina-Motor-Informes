# `_32.1` · Addendum — los `cc_*` salen de `resumen_metricas_dinamico`

> Addendum al `2026-08-12_32_pulido_manana.md`. **No lo edita.** Reemplaza su A.1 y su Parte B; el
> resto queda igual.
>
> **Reemplaza también la versión anterior de este mismo `_32.1`**, que apuntaba a
> `digital/Nomalización de barrios`. Esa pista salió del fixture del 06/08, que **no traía la Base
> Looker**. El usuario entregó el fixture del 31/07, que sí la trae, y ahí está la fuente.

---

## Lo medido

Sobre `Base Looker.xlsx` del fixture 31/07, por Claude, **desde afuera**. Vale como hipótesis
verificable, no como dato: la Parte A la reproduce contra la base viva.

**La fuente no es una solapa nueva: es `resumen_metricas_dinamico`, la que el motor ya lee.** Tiene
`call_enviado`, `call_discado`, `call_contactados` y `call_efectivos`, al lado de los `ivr_*` que
ya están cableados.

Medido sobre la cuenta `3289-JUNJDGAG` —*PRIMERA PERSONA | JM | PAULA PARETTO 27/7*, que es Villa
Urquiza—:

| columna | valor | caso |
|---|---|---|
| `call_enviado` | 6673 | — |
| `call_discado` | **6011** | `V-64` |
| `call_contactados` | **1878** | `V-65` |

`1878 / 6011 = 31,2 %`, que es `V-66`. **`cc_base` es `call_discado`, no `call_enviado`.**

Hay además una solapa `CC` —`ID Cuentas`, `Base enviada`, `Base barrida`, `Contactados`,
`Efectivos`— cuyos `Base barrida` y `Contactados` dan **los mismos 6011 y 1878** para esa cuenta.
Es la fuente desagregada del rollup. **No hace falta: usar la que el motor ya lee evita un `MAPEO`
nuevo tres horas antes de la demo.** Queda anotada como la desagregada, por si alguna vez se
necesita el detalle.

Eso también explica por qué Code no encontró nada: buscó `looker/CC x Cuentas`, y ese nombre no
existe en ningún lado.

⚠ **Lo que sí hay que mirar antes de cablear:** esos tres casos miden **una cuenta, un encuentro**.
**No miden la semana.** Sumando las cuentas con `fecha_fin` en 24–31/07 dan 56.726 y 14.711 sobre 15
cuentas, y de esas sólo dos son JM por nombre de campaña (`R-23`): la de Paretto y
`3387-JULJDGGC`, *TE CUENTO BS AS JM | 28/7 ORDEN PÚBLICO*. Las otras trece son *RDV Ministros*.

Es decir: `V-64`/`V-65`/`V-66` **validan la elección de columna, no el universo del agregado.**

---

## Parte A.1 · Reemplazada — reproducir contra la base viva

**Modelo: Sonnet, effort alto.** Sólo lectura.

1. En `looker/resumen_metricas_dinamico` vivo: confirmar que existen `call_discado` y
   `call_contactados`, y **si ya tienen fila en `MAPEO`**. Si la tienen, la Parte B es sólo
   `MARCADORES`.
2. Reproducir la fila de `3289-JUNJDGAG`: `call_enviado`, `call_discado`, `call_contactados`.
   **Si `call_discado` no da 6011 o `call_contactados` no da 1878, parar y reportar** — las bases se
   mueven, y si esta fila se movió hay que revisar la hipótesis de columna antes de cablear.
3. Con la ventana 24–30/07 y el corte JM de `R-23` aplicado: **cuántas cuentas quedan y cuánto suman
   `call_discado` y `call_contactados`**. Ése es el número que publicaría la lámina 5.

**Reportar y parar.**

---

## Parte B · Reemplazada — cablear los tres

**Modelo: Opus, effort alto.** Publica números.

**Sólo si A.1 reprodujo 6011 / 1878.**

Tres filas de `MARCADORES` contra `looker/resumen_metricas_dinamico`:

- `cc_base` → `call_discado`, `SUMA`
- `cc_contactados` → `call_contactados`, `SUMA`
- `cc_contact_pct` → `call_contactados / call_discado`, `PCT` — la etapa sobre la anterior,
  calcando la forma de los porcentajes que ya existen. **No se inventa operación.**

Más las filas de `MAPEO` que falten, y sólo ésas.

Las tres con `notas` = `SIN VALIDAR — demo 12/08`, y con la columna elegida escrita en la nota, para
que la ventana de validación sepa contra qué mirar.

**La ventana se resuelve como ya se resuelve para esa solapa.** No inventar un recorte propio para
estos tres: si el mecanismo que usan los marcadores vecinos no les sirve, **parar y reportar**.

**`alcance` y `clics` siguen en `—` hoy.** La misma base los tiene (`ALCANCE`, `DIGITAL`) y por eso
mismo: elegirles universo tres horas antes de la demo es la decisión que no se toma. Se anota que
son las candidatas y se deja.

---

## El resto del `_32`

Partes C, D, E y F sin cambios. Y sigue en pie: **nada puede empeorar `jm-20260811-234158`.**

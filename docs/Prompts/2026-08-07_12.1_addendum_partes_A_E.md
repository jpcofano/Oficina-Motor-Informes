# `12.1` · Addendum al prompt `2026-08-07_12_distinct_barrios.md` — luz verde a las Partes A–E

**Luz verde.** La Parte 0 corrió y las tres cosas que la frenaban se resolvieron: el universo de
la lámina 5 quedó cerrado por el `_13`, `0.2` confirmó que `R-10` no alcanza a los valores y el
mecanismo de la caja quedó medido. Se sigue con A, B, C, D y E, con los siete ajustes de abajo.
El prompt original no se edita: su Parte 0 ya corrió.

**Cero `.gs`, cero corridas.** Sigue siendo documentación.

---

## Los siete ajustes

`AJ-1` · **Los IDs, medidos hoy:** el `_13` cerró con un **addendum a `R-15`**, no con una regla
nueva, así que **`R-18` sigue libre** y es el de `A.1`. Para `B.1`, el máximo escrito en
`SUPUESTOS.md` es `S-03`, así que sería **`S-04`**. **Confirmarlo antes de escribir, no
asumirlo.**

`AJ-2` · **`0.4` hay que medirlo de nuevo, y ahora tiene que dar 4.** Los 11 barrios que midió la
Parte 0 salían de las 15 filas de doce figuras. Con el filtro `figura=Jorge Macri` puesto —seis
celdas escritas y verificadas por el camino del motor— los barrios de la ventana tienen que ser
**los 4 de Jorge Macri: Belgrano, Caballito, Retiro, Villa Urquiza**. **Re-medirlo y reportar el
número antes de escribir `C.2`.** Si no da 4, parar: el filtro no está alcanzando a esta lectura.

`AJ-3` · **`C.2` cambia de contenido y mejora.** Ya no registra una discrepancia sin explicar
entre *"3 a 6, promedio 4"* y once: registra que **la discrepancia tenía causa, la causa era el
universo, y quedó corregida el 07/08**. El dato del usuario y el del motor **coinciden**, y eso
es lo que se escribe. Un número del equipo que cierra con el motor vale más como control que
como anécdota.

`AJ-4` · **La fila de `ecv_barrios` nace con el filtro puesto.** Cuando se cablee —que **no es
esta corrida**— su fila de `MARCADORES` lleva `filtro = figura=Jorge Macri`, igual que las seis
del `_13`. **Escribirlo en la regla de `A.1` como parte del contrato de la operación**, no como
nota: una lista `DISTINCT` hereda el universo de su sección, y si nadie lo declara, cuenta de
más. Es el mismo error que acabamos de corregir, y el `DISTINCT` es el candidato más obvio a
repetirlo.

`AJ-5` · **Espacios internos: no se crea un quinto normalizador.** `0.3` midió que `normalizar_`
pliega case y acentos y hace `trim()`, pero **no colapsa espacios internos**, así que
*Villa Gral Mitre* con doble espacio no colapsaría. Hoy el caso **no existe**: cero pares
colapsan y los once valores están escritos limpios. **La regla usa `normalizar_` y escribe el
límite**, con la salida declarada: si aparece, se compone con el colapso de espacios y se anota
el motivo arriba de la función, como pide `CLAUDE.md` §2. **Inventar el normalizador hoy es
resolver un problema que nadie tiene.**

`AJ-6` · **La parada de `C.1` se disparó, y no cambia el plan.** `0.6` midió que los cuatro
tokens son **celdas de una misma tabla**, no cajas separadas, y que **`D-22` aplica**: el motor
lee tablas y no sabe agregarles filas. No hay mecanismo de desborde —`items_por_lamina` existe
como columna y en el seed, y **ningún consumidor la lee**—. Entonces: **la decisión de una caja
por barrio se escribe igual, y su implementación es lámina nueva y otro prompt.** **No tocar la
plantilla en esta corrida.**

`AJ-7` · **`D.3` tiene material.** `0.7` encontró que el nombre de la solapa del catálogo vive en
una constante de `Union.gs`, mientras el comentario de `catalogoBarriosDesdeBase_` dice que sale
de la fila de `BASES`. **Se anota como hallazgo de consistencia y no se arregla acá.** Y un dato
más para la misma entrada: existe un `{{ecv_barrio}}` singular en la lámina 6, que es un token
distinto de los cuatro y ya tiene su propio pendiente por el problema de prefijo.

---

## Lo que no cambia

`A.1` va como está, con la forma publicada saliendo del catálogo `Comunas` vía `parsearBarrio_`,
lo que no matchea a `REVISAR` y al listado de faltantes, orden alfabético, sin truncar, y cero
filas a `sin_datos`.

`A.2` va como está y ahora está medido: `0.2` transcribió el enunciado de `R-10` y confirmó que
habla de encabezados —el segundo "valor" del enunciado es el de `MAPEO`, que también es un
nombre de columna—. **No hay regla en contra y no hay de qué apartarse.**

`B.1` va como está: el supuesto de que los barrios y sus variantes no cambian es lo que autoriza
que las 11 variantes vivan en `Parseo.gs`.

`D.2` va como está y es la corrección que más vale del prompt: el `P2` decía que ésta era la
única decisión con una regla en contra, y era falso.

## Commits

Uno por archivo tocado. Documentación. Sin `—`. `git push` después de cada uno.

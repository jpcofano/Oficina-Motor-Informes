# `_43.2` · El CSV consolidado, y `X-21` vuelve — mi retractación estaba mal

> **Modelo por parte, declarado en cada una.** Sin declaración, Sonnet.
>
> **Reemplaza la sección "Lo que se cae" del `_43.1` y su Parte C.3.** La Parte B del `_43.1`
> —`SOLAPAS.campo_id_cuenta`— **sigue en pie sin cambios**: es la que hace falta, y ahora hace más
> falta que antes.

---

## Lo que pasó

El usuario aportó `casos_validacion_CONSOLIDADO_2026-08-12.csv`, **187 casos**. Contra los tres CSV
del repo: **`docs/casos_validacion_2026-08-12_addendum.csv` está truncado — tiene 22 de 40 filas**,
y las 18 que faltan son justo las de la base nueva: `V-86`–`V-90`, `C-50`–`C-58`, `A-12`,
`X-20`–`X-22`, más `X-06`–`X-09`, que el consolidado repone con estado `retractado` según el
criterio de `C-56`. **Ninguna fila del repo falta en el consolidado.** El truncamiento es de
respaldo, no de contenido: el archivo completo estaba en el disco del usuario.

Así que `X-21` **existe y está abierto**, y lo que retracté en el `_43.1` era el ID correcto de un
caso real. Lo di por inexistente porque grepeé contra un archivo incompleto y no me pregunté si el
archivo lo estaba.

**Y con eso vuelve la pregunta, que es lo que importa.** `X-21` dice que `3387` tiene tres filas en
`looker/CC` —barrida 6.977 / 255 / 722— y que **la lámina 11 publica 7.232, que son dos de las
tres**. La suma de las tres da 7.954. Entonces:

- **`SUMA` por cuenta sobre `looker/CC` no reproduce el número de referencia.** Lo que el `_43`
  midió —que la suma de las filas cierra contra `call_discado` de `resumen_metricas_dinamico`— sigue
  siendo cierto y no alcanza: dice que las dos solapas de `looker` coinciden entre sí, **no** que
  coincidan con lo publicado.
- **Los `enc_*` de Call Center no quedan destrabados.** No se cablean en el `_44`.
- Lo que sí queda declarado, por `C-58`: **el embudo del iceberg se cablea contra `Agenda JM`**, con
  `REVISAR` cuando el bloque venga en cero, y **los `cc_*` siguen en `looker/CC x Cuentas`**.

---

## Parte A · Sólo lectura

**Modelo: Sonnet. Effort: normal.**

1. Contar las filas de `docs/casos_validacion_2026-08-12_addendum.csv` y listar sus `caso_id`.
   Confirmar el truncamiento contra el consolidado que trae el usuario.
2. **¿Se ejecutó el `_43.1`?** Si sí, decir qué quedó escrito en su Parte C.3 —el addendum al
   handoff de sesión que declara inexistentes a `X-20`, `X-21` y `X-22`— y en qué commit.
3. Grepear `CC x Cuentas` en `docs/` y en las hojas vivas de `SOLAPAS`. El censo del `_40` reportó
   que **esa solapa hoy no existe** y `C-58` la nombra como destino de los `cc_*`. **Reportar cuál
   de las dos cosas es cierta hoy, sin resolverlo.**

**Reportar y parar.**

---

## Parte B · El consolidado entra al repo

**Modelo: Sonnet. Effort: normal.**

1. Commitear `casos_validacion_CONSOLIDADO_2026-08-12.csv` en `docs/`, **sin editarle una línea**.
2. **No borrar ni editar los tres CSV previos.** El consolidado declara en el commit a cuáles
   reemplaza —los tres— y que ninguna fila de ellos falta en él. La cadena de reemplazo es por
   declaración, no por borrado (`CLAUDE.md` §7).
3. Si la Parte A.2 encontró el addendum del `_43.1` ya escrito, **corregirlo con otro addendum
   fechado** que diga que los tres casos existen y que el archivo consultado estaba truncado. **El
   texto anterior no se edita.**

---

## Parte C · Lo que esto cambia, anotado

**Modelo: Opus. Effort: alto.** Decide si un token se cablea o no.

En `docs/PENDIENTES_consistencia.md`:

1. **`X-21` sigue abierto y ahora tiene una medición del motor al lado:** la suma de las tres filas
   de `looker/CC` de `3387` da 7.954 y cierra exacta contra `call_discado` de
   `resumen_metricas_dinamico`, mientras la referencia publica 7.232. **Las dos solapas de `looker`
   concuerdan entre sí y discrepan de lo publicado.** Por lo tanto ninguna operación sobre
   `looker/CC` —ni `SUMA` ni elegir una fila— tiene hoy con qué reproducir el número, y **los `enc_*`
   de Call Center no se cablean.**
2. **Corregir lo que el `_42` dejó anotado** si contradice esto, con addendum fechado y no editando.

---

## Reporte final

- Qué devolvió A.3 sobre `CC x Cuentas`.
- Qué quedó en el repo y qué addendum se escribió.
- Si al leer el consolidado apareció algún caso que contradiga algo ya anotado en `PENDIENTES` —
  nombrarlo, sin arreglarlo.

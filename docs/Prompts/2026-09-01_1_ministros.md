# `2026-09-01_1` — Ministros: los diez `emin_*` de `L-012` (`secco`)

**Subagente:** ninguno.
**Destino:** `docs/Prompts/`.
**Estado:** no ejecutado.

---

## El hecho

`L-012` de `secco` —*Encuentros de ministros*— salió en la corrida del 01/09 con **todos sus
tokens en `/////`**, o sea sin fila en `MARCADORES`. Son diez: `emin_alcance`,
`emin_alcance_semanal`, `emin_aperturas`, `emin_clics_ctor`, `emin_clics_ctr`, `emin_ctor`,
`emin_ctr`, `emin_encuentros`, `emin_lista`, `emin_or`.

Es lo único que le queda a `secco` además de tres tokens sueltos: campañas y reuniones ya cierran
con los 168 en `*`.

**Lo que la lámina pide, leído del deck generado:** alcance semanal, mails entregados, aperturas
(OR), clics (CTOR), cantidad de encuentros, «encuentros contempladas» con su lista, alcance,
impresiones y clics (CTR). Más un pie de benchmarks —*Mail OR 22 %*, *Digital CTR 0,2 %*— que
**está escrito a mano en la plantilla y no es un token**.

⭐ **Y el dato del usuario, que es el punto de partida y no una conclusión:** la lámina toma los
encuentros de la semana **de los ministros** y los presenta ahí. La fuente probable es
**`reuniones` → `Agenda funcionarios`**.

---

## Parte 0 — sólo lectura

**Modelo: Sonnet. Effort: alto.** ⛔ **No editar. Reportar y parar.**

### 0.1 · Las dos candidatas, y hay que elegir midiendo

`SOLAPAS` declara **dos** solapas de encuentros que no son de JM, las dos ya censadas:

- **`reuniones` → `Agenda funcionarios`** · `uso = fuente` · nota: *«encuentros de otros
  funcionarios — mismo caso que `rdv/RDV_otros_ministros`»*
- **`rdv` → `RDV_otros_ministros`** · `uso = fuente` · nota: *«mapeada; base ajena, ojo con la
  firma»*

⇒ Medir **las dos**: qué columnas tiene cada una, cuántas filas, y **cuál puede responder los diez
tokens**. ⚠ La segunda ya tiene un campo mapeado (`fecha_periodo`, col E) y la primera **ninguno**,
así que «está mapeada» no es lo mismo que «es la fuente correcta» — el usuario nombró la primera.

⚠ **Y la nota de `RDV_otros_ministros` dice «base ajena, ojo con la firma»:** verificar la
`firma_encabezado` contra `SOLAPAS` antes de citar cualquier columna. Si cambió, **eso es el
hallazgo** y todo lo que se mida encima sale corrido sin fallar.

### 0.2 · Los diez tokens contra las columnas reales

Para cada uno de los diez: **qué columna lo daría, con qué operación**, o **⛔ no hay columna que
lo dé**. Sin inventar la correspondencia: si un token no tiene fuente clara, **eso se reporta como
falta y no se resuelve con la columna que más se le parezca**.

⚠ **Dos que van a necesitar cuidado y conviene mirarlos aparte:**
- **`emin_lista`** es una lista de nombres, no un número. `m2_camp_lista` ya resuelve un caso así
  con `LISTA_CRUDA` — mirar cómo, en vez de diseñarlo de cero.
- **`emin_encuentros`** es un conteo de encuentros, y `CUENTA_DISTINTOS` sobre la columna
  equivocada cuenta filas en vez de encuentros. `m2_campanias` tiene ese mismo problema resuelto y
  documentado.

### 0.3 · Lo que ya está validado, y hay que buscarlo antes de cablear

⭐ **`D-56`: el CSV manda.** Y el CSV **ya tiene casos de ministros**: `V-49` (`emin_encuentros`,
valor 8), `V-50`, `V-51`, `V-52` (`emin_inscriptos_*` y `emin_asistentes_*` de agosto).

⇒ Leerlos enteros **antes de proponer nada**: dicen de qué solapa salieron, con qué corte y con qué
ventana. ⚠ **Si esos casos apuntan a una solapa distinta de la que el usuario nombró, eso se
reporta y se para** — no se elige una de las dos por cuenta propia.

⚠ Y ojo con los nombres: `emin_inscriptos_szinny_0508` **no es** ninguno de los diez. Puede ser un
token viejo, una convención distinta, o un caso escrito por persona y no por marcador. **Decir cuál
de las tres, no asumirlo.**

### 0.4 · La ventana y el universo

`R-21` dice que el temario selecciona. **¿La lámina de ministros se rige por el temario, o por la
ventana del informe?** El deck dice *«Semana del vie 21/08 — vie 28/08»*, o sea que al menos el
encabezado es la ventana. **Medirlo, no deducirlo**, y decir qué implica para el `periodo_ref` y el
`ventana_ref` de las diez filas.

**Reportar y parar.** ⛔ **Ninguna fila de `MARCADORES` se escribe en esta parte.**

---

## Parte A — la propuesta de cableado

**Modelo: Opus. Effort: alto.** Diez números nuevos en un informe que empieza a publicarse.

Las diez filas propuestas, **una por una y con su justificación medida**: base, solapa, campo,
operación, filtro, `ventana_ref`, formato. Y para cada una, **qué caso del CSV la validaría** — o
⛔ **ninguno**, que es la respuesta honesta y la que corresponde a un cableado nuevo.

⭐ **Todas nacen con `_revisar`**, porque `D-56` dice que sin caso en el CSV no hay validación. ⚠
**Y nacen con `informe_id = 'secco'`, no `*`:** `jm` no tiene esta lámina, así que compartirlas no
tendría sentido y además no hay dónde comprobar el número del otro lado.

⛔ **No escribir todavía.** El usuario aprueba la propuesta antes de que se toque `MARCADORES`.

---

## Parte B — aplicar, con el visto bueno

**Modelo: Sonnet.**

Backup de `MARCADORES` primero, y **backup fallido aborta sin escribir una celda**. Relectura de la
hoja después. Banco con control positivo y negativo con mutación verificada. Commit propio,
`docs/BITACORA.md` en una línea.

⛔ **`R-20` es código, no configuración.** Si alguno de los diez lo necesita, **eso NO va en este
prompt**: se reporta y se abre uno propio.

---

## Lo que este prompt NO hace

- No toca la plantilla ni el pie de benchmarks, que es texto del equipo (`C-01`).
- No toca los siete `m2_*`, que esperan su caso en el CSV.
- No decide entre las dos solapas por cuenta propia: la Parte 0 mide y el usuario elige si hay duda.

# Paso 2026-08-21_18 — El alcance del 1 a 1 sale de `reuniones`, una solapa por etapa

**Estado:** no ejecutado.
**Reemplaza:** nada.
**Toca:** las hojas `MARCADORES` y —quizá— `SOLAPAS`, `docs/PENDIENTES_consistencia.md`, y el CSV
de casos. **No toca ningún `.gs`.**

---

## Contexto

`u1_pre_meta_alcance` y `u1_post_meta_alcance` salen `-` porque apuntan a `digital/Alcance`, que
no puede separar las etapas. Está escrito como hueco (`Addendum_2026-08-21_Paso-15`).

**La salida no necesita dimensión ni columna nueva.** En `reuniones` el par pre/post está separado
**por solapa**, y `alc_real` ya está mapeado en las dos:

| solapa | columna | encabezado |
|---|---|---|
| `Agenda JM` | `AF` | `Alcance manual` |
| `Agenda JM \| Post` | `G` | `Alcance` |

Es la forma que `D-30` punto 1 cita de `C-50`: el par comparte `ID` en dos solapas distintas, y la
clave es `(ID, solapa)`. **Confirmado por el usuario, 21/08:** `alc_real` es el alcance de los
encuentros — los `1 a 1` y los demás. **Ningún marcador lo usa todavía.**

---

## Parte 0 — verificación de premisas · **Sonnet** · sólo lectura · reportar y parar

### ⛔ 0.1 · Un conflicto de medición, y es lo primero

**Hay dos mediciones incompatibles sobre `SOLAPAS.campo_id_cuenta` de las dos solapas de
`reuniones`, y el paso entero depende de cuál es cierta.**

| fuente | dice |
|---|---|
| `docs/_snapshots/SOLAPAS_2026-08-20.tsv`, `_2026-08-21.tsv`, `_1826` y `_1915` | `id_cuenta` en las dos |
| `SEED_SOLAPAS_` (`Instalar.gs`), las dos filas de `filaSolapa_('reuniones', …)` | `campo_id_cuenta: 'id_cuenta'` |
| Medición de la hoja viva, 21/08, después de correr *Aplicar configuración* | **vacío en las dos** |

**Remedir la hoja viva, con cuidado y reportando la fila cruda entera**, no sólo la celda:
`campo_id_cuenta` es la **columna 11 de 12** y una lectura desplazada la confunde con
`ventana_ref` (10) o `notas` (12), las dos vecinas y las dos plausibles.

**Si la remedición da `id_cuenta`:** la medición anterior estaba desplazada, se anota y se sigue —
el radio de este paso es **cero** y son dos celdas de `MARCADORES`.

⛔ **Si la remedición confirma vacío, PARAR y no escribir nada, ni siquiera `SOLAPAS`.** Sería una
divergencia hoja↔seed en una columna que el sembrador **sí** incluye en su objeto, en filas con
`origen = seed` (o sea, sin la protección de `manual`), y **detectada justo después de correr
*Aplicar configuración***. Eso no es una celda para rellenar: es un hallazgo sobre el sembrador,
va a `docs/PENDIENTES_consistencia.md` con las tres fuentes y sus fechas, y **rellenar la celda
borraría la evidencia antes de que nadie mire por qué se vació**. `CLAUDE.md` §7 es explícito:
hoja contra sembrador no es una contradicción a dirimir, es un hallazgo.

⚠ **Y el radio depende de esto, así que no se puede estimar antes.** Si hoy la hoja **no** declara
`campo_id_cuenta`, declararlo le cambia el camino de lectura a los marcadores que ya viven en
`Agenda JM` —los `cc_*`, `enc_alcance_potencial`, `enc_alcance_pct`, `enc_impresiones`— que
pasarían de la rama general a la rama por cuenta. Es la *intersección peligrosa* que el `_15` A.3
midió para `digital`. **Si la hoja ya lo declara, esos marcadores ya leen por cuenta y no se mueve
nada.** Contarlos y reportarlos igual, en los dos escenarios.

### 0.2 · `Alcance manual` — cuánto está cargado

El encabezado de `Agenda JM` col AF dice **manual**, y la nota del seed lo explica: *"Toda la
solapa es carga a mano: 0 fórmulas en 44 columnas × 154 filas"*. **No es una anomalía de esa
columna: es la solapa entera.**

Lo que igual hace falta es **la tasa de carga** — cuántas filas con `mostrar = sí` tienen
`alc_real` cargado, en cada solapa. Si está mayormente vacía, los tokens publican `-` en casi todas
las láminas y el paso no rinde. **Requiere ver la base y Code no tiene acceso**: reportarlo como
medición pendiente del usuario, no intentar rodeos.

### ✅ 0.3 · Etapa o plataforma — **CONTESTADO, no hay nada que medir**

**Decisión del usuario, 21/08: el único alcance que existe es el de Meta.** No hay alcance de
Google ni de programmatic, en ninguna de las dos solapas.

Con eso:

- El `meta` de `u1_pre_meta_alcance` y `u1_post_meta_alcance` **no es vestigial: es correcto**, y
  el casillero de la plantilla no hace falta abrirlo.
- **Concuerda con lo que ya estaba medido** y le da sentido: la nota de `SEED_SOLAPAS_` para
  `reuniones/Agenda JM | Post`, del 14/08, dice que la columna `G` *"NO es por plataforma y su
  banda «Acumulado» está mal rotulada: es el de Meta"*. **No hay nada que acumular** — es la única
  plataforma que lo trae.
- `AF` en `Agenda JM` es el de Meta por el mismo motivo, y ya no hace falta resolverlo por
  simetría.

**Lo único a verificar acá es que la nota del seed siga diciendo eso.** Reportar y seguir.

### 0.4 · Qué más se movería

Listar todo marcador que hoy lea `alc_real`. La premisa es **cero**. Si aparece alguno, reportar
antes de tocar nada.

**Reportar y parar.**

---

## Parte A — las dos filas de `MARCADORES` · **Opus** · effort alto

Mueve dos números publicables. **Sólo si 0.1 se resolvió a favor de `id_cuenta`.**

| marcador | `base_id` | `solapa` | `campo_logico` | `operacion` |
|---|---|---|---|---|
| `u1_pre_meta_alcance` | `reuniones` | `Agenda JM` | `alc_real` | `ULTIMO` |
| `u1_post_meta_alcance` | `reuniones` | `Agenda JM \| Post` | `alc_real` | `ULTIMO` |

- **`dimensiones` queda vacío en los dos, y ahora es correcto.** El corte lo hace la solapa, no una
  dimensión. Antes vacío significaba *"no hay nada que separe"*; ahora significa *"no hace falta"*.
  Es el mismo texto con otro sentido, así que **va dicho en `notas`**.
- **`formato = miles_revisar` se conserva.** El número no está validado. **No lo saques.**
- **`filtro` sigue vacío.** El recorte lo hace la cuenta del ítem (`D-30` punto 2).

**El control es que los dos publiquen números distintos.** Si salen iguales, el corte por solapa no
funcionó y el cableado no sirve — es la falla que este paso viene a evitar. Reportar los dos
valores, el encuentro del que salieron y el `origen` que devolvió la rama.

⚠ **No se corre para verificar: se reporta y el usuario corre.** Este paso escribe configuración.

---

## Parte B — cerrar el hueco y abrir el caso · **Sonnet**

**B.1** — en `docs/PENDIENTES_consistencia.md`, el hueco escrito por el
`Addendum_2026-08-21_Paso-15` pasa a resuelto **por addendum fechado**, no editando el texto: qué
se hizo, con qué evidencia, y que la pista de `reuniones` era correcta y salió más barata de lo
anotado —el par por solapa ya estaba mapeado—.

**B.2** — dos casos de validación nuevos en el CSV, uno por token. **Números libres de su serie, sin
reusar** — reportar cuáles tomaste. Estado **pendiente**, no `exacto`: un caso `exacto` cita la
lámina del deck y todavía no hay corrida.

⚠ **El caso tiene que decir qué afirma, que no es sólo el número: que el PRE y el POST son
distintos y salen de solapas distintas.** Un caso que sólo registra un valor no habría detectado el
bug anterior — los dos tokens publicaban lo mismo y cada uno por separado se veía bien.

**B.3 · La regla, escrita donde vive.** *El único alcance que existe es el de Meta* es un hecho
del dominio, no una decisión de este paso: va a `docs/REGLAS_NEGOCIO.md` como `R-NN` nuevo —
número libre, append-only, con fecha y con el usuario como dueño (21/08). Greppear antes: si ya
está dicho, cero ediciones y se registra el cero.

**Y la regla tiene que decir sus dos consecuencias**, porque son las que la vuelven útil:

1. **Explica el `meta` en los nombres de los cuatro tokens** y la banda `Acumulado` mal rotulada
   de `Agenda JM | Post` — no hay nada que acumular.
2. ⚠ **`u1_total_frecuencia` mezcla universos, y hay que dejarlo anotado antes de que alguien lo
   cablee.** El deck lo publica como `impresiones / alcance` = 377.997 / 55.255
   (`PENDIENTES`, 21/08), o sea **impresiones de las tres plataformas sobre alcance de una sola**.
   Con esta regla eso deja de ser una sospecha y pasa a ser un hecho sobre la definición. **No se
   resuelve acá** —el token está entre los ocho `/////` deliberados— pero sin la nota, el día que
   se cablee va a parecer una división obvia.

---

## Fuera de alcance

- **`u1_total_alcance` y `u1_total_frecuencia`.** Siguen entre los ocho `/////`: el alcance son
  usuarios únicos y no se suma. Que aparezca `alc_real` no los resuelve.
- **`alc_potencial` y `alc_cobertura_pct`.** Ya tienen sus tokens y no se tocan.
- **Renombrar los tokens.** La plantilla es del equipo (`C-01`), y un rename es por `informe_id`,
  nunca global. Y con 0.3, el `meta` del POST resultó ser correcto.

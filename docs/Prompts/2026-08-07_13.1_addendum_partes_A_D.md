# `13.1` · Addendum al prompt `2026-08-07_13_universo_de_figura.md` — luz verde a las Partes A–D

**Luz verde.** La premisa de la que colgaba todo está confirmada por el usuario: **la lámina 5
del informe `jm` cuenta sólo Jorge Macri.** Se sigue con A, B, C y D, con los siete ajustes de
abajo. El prompt original no se edita: su Parte 0 ya corrió.

---

## Los siete ajustes

`AJ-1` · **Son seis marcadores, no ocho, y los siete que faltan son el hallazgo.** La tabla de
`C.1` va con las seis filas reales —`ecv_encuentros` y los cinco `_pct`—. Los otros siete que el
prompt nombraba **no tienen fila en `MARCADORES`**: son los numeradores sin cablear, que es lo
que produce el `Mail: «FALTA:ecv_insc_mail»(59.9%)` ya anotado. **Escribirlo así**, porque
cambia el sentido del hallazgo: no es que el deck publique ocho números mal, es que publica
**seis mal y siete huecos**, y los huecos **van a nacer mal** si se cablean antes que el filtro.

`AJ-2` · **El mecanismo es `MARCADORES.filtro` y `B.2` se escribe sin condicionales.** La columna
existe, está vacía en las seis filas, y las seis declaran `informe_id = jm`. **Lo que hay que
dejar escrito no es la elección sino el descarte:** `SECCIONES.filtro` se descarta **no por lo
que haría hoy** —`SECCO` no tiene ningún marcador sobre `rdv`, así que hoy no pasaría nada—
sino por el día que los tenga. Un filtro que no molesta hoy y rompe callado en tres meses es
peor que uno que falla ahora. **Esa frase es la que vale del párrafo.**

`AJ-3` · **`B.2` se acota a `ecv_alcance_semanal`.** `0.6` desmintió la premisa de que `encuentro`
necesitara el mismo filtro: itera sobre `REUNIONES`, que es una hoja curada **sin columna
`figura`**, y no lee `rdv`. La única sección que cambia de número es el agregado, con sus seis
marcadores. **Escribir también el motivo**, para que nadie lo vuelva a intentar: el filtro por
figura no se puede declarar sobre una fuente que no tiene la columna.

`AJ-4` · **La pregunta que abrió `0.6` va a `PENDIENTES_consistencia.md`, no se responde acá.**
Las 7 filas de `REUNIONES`: ¿son todas encuentros de JM o incluyen ministros? **Sin columna
`figura` no se puede saber desde la hoja**, y afecta a la lámina 6, que se repite por reunión.
Es **pregunta para el equipo**, con su sección propia en ese documento. No inventar la respuesta
y no deducirla de que hoy sean siete.

`AJ-5` · **Antes de `D.1`, un chequeo de una línea y seguís.** Confirmar que
`MARCADORES.filtro` usa la misma sintaxis que `SECCIONES.filtro` —`aplicarFiltroDeMarcador_`
llamando a `parsearFiltro_` con `campo=valor`, y el campo resuelto por `buscarMapeo` contra la
base y solapa **del marcador**—. Si coincide, escribís `figura=Jorge Macri` en las seis celdas y
seguís. **Si no coincide, parás**: la celda queda sin escribir y se reporta.

`AJ-6` · **`C.1` lleva la explicación de por qué tres porcentajes SUBEN al filtrar.** Es
contraintuitivo y sin el motivo escrito alguien lo va a "arreglar" en seis semanas: `insc_cc` y
`insc_ivr` tienen **el mismo numerador en las dos columnas** —ese canal es 100 % de JM— y el
denominador cae de 3344 a 2307. **Filtrar no baja todo: redistribuye.**

Y una precisión que hay que dejar dicha: **`ecv_insc_dif_pct` pasa a 0, y ese 0 es un dato, no
un `sin_datos`.** Las 71 inscripciones diferidas son todas de otros ministros. Cuando se cablee
`ecv_insc_dif`, tiene que publicar **cero**, no un hueco — son dos estados distintos y el motor
los distingue a propósito.

`AJ-7` · **El orden queda escrito, y no como recomendación.** Los cinco numeradores `ecv_insc_*`
y `ecv_inscriptos` / `ecv_asistentes` **se cablean después del filtro, nunca antes.** Si se
cablean antes nacen con el universo de doce figuras y el error se multiplica por siete en vez de
corregirse. Va en `C.1` junto al hallazgo, y es la misma razón por la que el subagente
`cableador` nace bloqueado.

---

## Lo que no cambia

`A.1`–`A.3` van como están: addendum a `R-15` —que **no tiene ninguno**, así que es su
`Addendum 1`— con la quinta fila, `rdv/RVD JM-CM - ES` columna `A`, `JM` = *Jorge Macri* literal
—`0.4` confirmó que matchea exacto, sin variantes ni acentos—, `GCBA` por resta.

`C.2` va como está y ahora tiene más peso: ninguna verificación del proyecto miraba el universo,
y el error pasó por tener fila, mapeo, fuente con filas y formato correcto. Es el origen de la
pregunta que el subagente `verificador` incorpora.

`D.2` no cambia: los seis marcadores tienen que dar la columna *sólo JM* de la tabla de `0.1`.
Si alguno no da, parar.

## Commits

Uno por parte. `git push` después de cada uno. La celda de `D.1` no es un commit: se anota en la
bitácora con lo que midió `D.2`.

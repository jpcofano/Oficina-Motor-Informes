# HANDOFF — Motor de Informes (GCBA) — 2026-07-30

> Para arrancar una conversación nueva con contexto completo. Trabajamos en español.
> Convención: archivo nuevo por sesión, fechado, nunca se edita uno anterior — ver
> `Plan Inicial/PROYECTO.md` §9. Historia completa del 29/07 en
> `docs/Sesiones/HANDOFF 2026-07-29.md` y `HANDOFF 2026-07-29-2.md`. Esto es solo lo
> nuevo de hoy.

---

## Paso 2.2.1 — parche del Paso 2.2 (`docs/Prompts/Paso-2.2.1.md`)

El usuario corrió "Armonizar tokens de plantillas" contra las Slides reales y confirmó
que **JM slide 5 y slide 6 quedaron correctas** (el criterio de "etiqueta + caja de valor
más cercana" funcionó, incluida la desambiguación del token `{{ivr_atendidos}}` duplicado
en dos cajas distintas). Aparecieron dos problemas nuevos, corregidos en commits
`c5d9f02` (Problema 1) y `e0cd96f` (Problema 2):

**Problema 1 — regresión en SECCO.** El renombre `enc_audiencia`→`enc_alcance` era
correcto en JM pero rompió SECCO (ahí `enc_audiencia` ya significaba lo correcto: la
columna IVR de la slide 8; después de la armonización, dos cajas de SECCO 8 quedaron con
`{{enc_alcance}}` — "Audiencia" y "Alcance" apuntando al mismo token). Causa raíz: un
único array de renombres se aplicaba a **todas** las plantillas por igual. Fix:
`RENOMBRES_ARMONIZACION_` pasó a ser `RENOMBRES_ARMONIZACION_POR_INFORME_` (objeto por
`informe_id`); si un informe no tiene lista, no se le toca nada y se avisa en el reporte.
Se restauró la caja "Audiencia" de SECCO por etiqueta (mismo criterio que B.1, porque
ahora hay dos cajas con `{{enc_alcance}}` y buscar por valor sería ambiguo).

**Problema 2 — dos cosas de la Parte B.4 original que nunca se aplicaron.** (a) Los 16
renombres de M2 slide 10 (`m2_clics_a`→`m2_subtes_clics` y compañía) **nunca se habían
codeado** — un vacío mío del Paso 2.2, no una regresión. Los agregué a la lista de `jm`
en `RENOMBRES_ARMONIZACION_POR_INFORME_`: son renombres de texto 1 a 1 seguros (cada
token viejo es único en la presentación, a diferencia del caso `enc_audiencia`), aunque
el mapeo real no siga un patrón simple (`m2_vis_e` está en Desalojos, `m2_camp1`/`camp2`
cruzados) — la tabla ya viene verificada contra la Sheet viva. (b) La limpieza de los 14
números fuera de canvas **sí estaba codeada pero no hacía nada**: usaba
`slide.getShapes()`, que no baja a elementos agrupados (`Group`); si esos números viejos
están agrupados, quedaban invisibles para el código. Reescribí
`limpiarCajasFueraDeCanvas_` sobre `getPageElements()` con recursión en `Group`, y agregué
`diagnosticoElementosSlide_(plantillaId, numeroSlide)` como herramienta de troubleshooting
(loguea tipo/posición/texto de cada elemento, recursando en grupos) para el caso de que
esto tampoco alcance.

**Nada de esto se corrió todavía contra la plantilla real** (sin acceso de ejecución
desde acá). Falta que el usuario:
1. Corra "Armonizar tokens de plantillas" de nuevo (con el parche aplicado).
2. Verifique las 6 búsquedas de `docs/Prompts/Paso-2.2.1.md` §"Prueba del usuario":
   `enc_audiencia` da un resultado en cada plantilla (no cero, no dos), `m2_clics_a`/
   `m2_vis_e` dan cero, `m2_subtes_clics`/`m2_desalojos_vis` dan uno cada uno (y este
   último hay que **mirarlo**, no solo buscarlo, para confirmar que cayó en la columna de
   Desalojos), y JM slide 10 sin números sueltos arriba del canvas.
3. Correr la armonización una tercera vez: 0 cambios en todo, o hay un renombre pisándose.
4. Si la limpieza de JM 10 **sigue** sin borrar nada, correr
   `diagnosticoElementosSlide_('<id de JM>', 10)` desde el editor y mirar el log — ahí se
   va a ver por qué (probablemente confirma o descarta la hipótesis del `Group`).

Con el parche confirmado: sacar la advertencia de `docs/TOKENS.md` (Parte D del Paso 2.2,
sigue pendiente a propósito) y recién ahí queda habilitado el Paso 2.5.

**Sigue sin resolver, no es de este parche:** `enc_audiencia_ivr`→`enc_base_total` sigue
listado como pregunta sin confirmar en `docs/PLANTILLAS_QA_y_armonizacion.md` §9 (pregunta
1), aunque el código ya lo aplica como parte de la lista de `jm` (porque `Paso-2.2.md` lo
pedía así). Vale la pena resolver esa pregunta antes de la próxima corrida, para no tener
que volver a parchear una plantilla ya editada.

---

## Paso 2.2.2 — plantilla canónica única, backup y no escribir sobre la del equipo (`docs/Prompts/Paso-2.2.2.md`)

Hallazgo del usuario, más grave que el 2.2.1: **hay dos presentaciones JM distintas en
Drive** (mismo nombre, orden de slides distinto — ya venían divergiendo antes de la
armonización). `INFORMES.jm.plantilla_id` apuntaba a `1JrHvs_p…`, que **no** es la que usa
el equipo — la canónica es `117I0qn1…`. Es decir: **todo lo armonizado en el 2.2/2.2.1 se
aplicó sobre el archivo equivocado**, uno que ni siquiera se usa. Y de fondo:
`armonizarPlantillas()` escribe directo con `SlidesApp.openById()`, sin copiar — cada
corrida es destructiva y sin vuelta atrás (ya costó la regresión de SECCO en el 2.2.1).

Commits `cf2c249` (A), `f75a059` (B), `b7a1524` (C), doc en `a842cb4` (E). **Parte D
(correr la armonización sobre la canónica) NO se hizo — está bloqueada a propósito**, ver
abajo.

- **A — `inventarioPlantillas()`** (menú "Inventario de plantillas"): por cada informe,
  nombre/ID/URL/carpeta/fecha de modificación, cantidad de slides, título de cada slide, y
  si contiene tokens viejos conocidos (responde "¿está armonizada?" de un vistazo). Esto
  hubiera hecho evidente el problema de entrada.
- **B — Backup obligatorio.** `armonizarPlantillas()` ahora crea una copia en una
  subcarpeta `_backups` (dentro de la carpeta de plantillas) **antes** de tocar nada; si
  el backup falla, aborta esa presentación sin escribir. El reporte muestra el ID/URL de
  cada backup arriba de todo.
- **C — Repuntar `INFORMES.jm`** a `117I0qn1…` (función `repuntarPlantillaCanonicaJM_()`,
  menú "Fijar plantilla canónica de JM (una vez)") y marcar `1JrHvs_p…` como
  `[OBSOLETA — no usar]` en Drive (no se borra: sirve de referencia de cómo debería quedar
  la canónica). **La Parte C.2 (que `registrarPlantillasDesdeCarpeta()` no pise un
  `plantilla_id` distinto ya cargado) ya estaba implementada desde el Paso 1.6 v2** — lo
  verifiqué leyendo el código antes de tocar nada, no hizo falta cambiar eso.
- **E — Doc.** `PROYECTO.md` §6: la regla de tres puntos ("la plantilla es del equipo, el
  motor se adapta") + tabla de plantillas canónicas con ID y estado. `docs/TOKENS.md`:
  corregido el ID de JM en la advertencia (decía `1JrHvs_p…`, que es justo la obsoleta).

**Parte D bloqueada — decisión pendiente del usuario, no la tomé por mi cuenta:** en la
matriz digital de M2 hay una caja `{{m2_salud_camp}}` **huérfana y visible** (no está en
la columna de Salud pese al nombre). Si se corre la armonización tal cual está, el
renombre `m2_camp4`→`m2_salud_camp` (parte de la lista de `jm`) crearía una **segunda**
caja con ese mismo token — el problema de `enc_audiencia` otra vez, esta vez en M2. El
prompt es explícito: "el usuario tiene que mirar esa caja y decidir" (¿es un sobrante para
borrar, o hay que sacar ese renombre de la lista?). **No corrí la Parte D** ni voy a
correrla hasta que esto se resuelva — de cualquier forma no tengo acceso de ejecución
contra Slides real.

**Próximos pasos concretos para el usuario:**
1. Decidir sobre la caja huérfana `{{m2_salud_camp}}` (mirar la slide de M2 en la
   plantilla canónica `117I0qn1…`).
2. Correr "Inventario de plantillas" para confirmar que `jm` apunta ahora a `117I0qn1…`.
3. Correr "Fijar plantilla canónica de JM" si `INFORMES.jm` todavía no se actualizó solo.
4. Recién con (1) resuelto: correr "Armonizar tokens de plantillas" sobre la canónica,
   confirmar el backup, y repetir las verificaciones de `Paso-2.2.1.md` — ahora sobre
   `117I0qn1…`.
5. Confirmar que `registrarPlantillasDesdeCarpeta()` reporta conflicto (no pisa) si
   `1JrHvs_p…` sigue visible en la carpeta de plantillas.

---

## Git al cerrar

Commits de hoy: `c5d9f02`, `e0cd96f` (Paso 2.2.1), `cf2c249`, `f75a059`, `b7a1524`,
`a842cb4` (Paso 2.2.2), más el de esta entrada de HANDOFF. Revisar `git log --oneline -10`
y `git status` antes de asumir qué está pusheado.

## Docs vivos (sin cambios respecto de ayer)

Ver `Plan Inicial/PROYECTO.md` §9 (taxonomía) y `docs/PENDIENTES_consistencia.md` para el
estado completo de qué doc es qué.

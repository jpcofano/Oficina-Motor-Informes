# HANDOFF — Motor de Informes (GCBA) — 2026-08-01

**reemplaza:** `HANDOFF 2026-07-31-3.md`

> ⚠ **Escrito por Claude Code, no por claude.ai.** `CLAUDE.md` §5 dice que Code no escribe
> nunca en `docs/Sesiones/`; se hizo por pedido explícito del usuario al cerrar la sesión.
> La regla y esta excepción quedan por reconciliar — anotado en
> `docs/PENDIENTES_consistencia.md`. Se deja marcada la autoría acá para que el dueño del
> archivo sea rastreable, que era el motivo original de la regla.
>
> Sesión de Code del 31/07–01/08. Dos mitades: se cerró `DOC-5` (orden documental) y se
> volvió al motor con el Paso 2.11 Parte C.2.
> **Trabajamos en español.**

---

## El árbol de trabajo — exacto, y deliberadamente sin commitear

`git status` al cerrar (verificado, sin archivos sin trackear ni sorpresas):

```
 M Fechas.gs
 M Instalar.gs
 M docs/BITACORA.md
 M docs/HANDOFF_CODE.md
```

Todo lo demás está commiteado y **pusheado** (`641ba42`). Los dos únicos archivos
ignorados son intencionales: `.claude/settings.local.json` (excludesfile global de la
máquina) y el `.zip` de muestras (`.gitignore`, decisión de datos fuera del repo público).

Qué contiene cada uno:

- **`Instalar.gs`** — Paso 2.11 Parte C (`calcularDiffUpsert_`, `menuAplicarConfiguracion_`,
  `menuEstadoConfiguracion_`, los sembradores partidos en núcleo sin `alert()` + wrapper)
  · C.2-1 (`normalizarParaComparar_` + `COLUMNAS_FECHA_REGISTRO_`, integradas en el diff)
  · el **retiro** de la migración `corregirNotaControlAnclaje_`.
- **`Fechas.gs`** — el diagnóstico de tipos `diagnosticoTiposFechasConfig_()`, que ahora
  releva también `BASES.fila_encabezado`.
- **`docs/BITACORA.md`** — la entrada del Paso 2.11 Parte C (con `commit <pendiente>`).
- **`docs/HANDOFF_CODE.md`** — la reescritura correspondiente.

**No commitear hasta que pase el protocolo.** Es una sola unidad: el diff y su
normalización se prueban juntos.

## Dónde quedó el protocolo

Protocolo: `docs/Prompts/Paso-2.11_ParteC2_diff_auditable.md`, siete pasos.
Corrida completa y evidencia: `docs/PROTOCOLO_2.11-C_corrida_2026-07-31.md`.

**5 de 7 pasos pasados.** Faltan **3, 4 y 5**, ahora con la migración ya retirada.

- Paso 1 (tipos de fecha) ✅ — las doce celdas son `Date`; la hipótesis de degradación de
  tipo quedó tumbada.
- Paso 2 (control positivo) ✅ — ver abajo, es el hallazgo grande.
- Pasos 6 y 7 ✅ — inventario con 84 filas reales, `filas_datos ≤ filas_crudas` en las 84.

**El criterio es la segunda corrida sin ninguna línea de cambio** (en particular, ninguna
de `PERIODOS` ni de `CAMPANAS` — criterio nuevo del addendum 2 al prompt, reemplaza al
`instanceof Date` que ya se cumplía y no discriminaba nada).

## Qué se cerró en esta sesión

- **`DOC-5` y el orden documental.** La tabla de autoridad —una pregunta, un dueño único,
  con quién escribe— quedó **instalada en `CLAUDE.md` §7**, con su desempate propio; la
  tabla de ruteo de §3 desapareció (eran dos mapas de autoridad y el desempate de uno
  apuntaba al otro). La propuesta que le dio origen está archivada en
  `Plan Inicial/_archivo/PROPUESTA_orden_documental.md`.
- **`PROYECTO.md` §7 dejó de llevar estado de avance** (llegó a estar diez sub-pasos
  atrás): es roadmap + puntero a `HANDOFF_CODE.md` y `BITACORA.md`. §9 dejó de asignar
  vivo/congelado centralmente: lo declara cada documento en su encabezado.
- **Campo único `reemplaza:`** para declarar reemplazo, mismo nombre en prompts y en
  handoffs, contemplando que un documento pueda ser reemplazado por varios.
- **El hallazgo que más costó: `BASES` y `MAPEO` sí se auditan.** El "cero líneas" de la
  primera corrida de la Parte C era **ausencia de cambios, no ceguera del diff** — lo probó
  el control positivo: con las ediciones puestas, `BASES` reportó
  `m2.hoja_default: Cuentas M2 → (vacío)` y `MAPEO` repuso la fila borrada. La duda venía
  abierta desde el 31/07.
- **Menú declarado por tabla** (commit `9fd16c6`) — ese commit por sí solo no corre:
  referencia tres funciones que todavía viven solo en el árbol.

## Qué sigue después del commit

**C.2-2 a C.2-7** del prompt, que es donde el diff se vuelve auditable de verdad: hoy el
diff no dice qué auditó (sin cabecera de corrida ni bloque de alcance), las migraciones
escriben por fuera de él, no hay línea `solo_en_hoja`, y las protegidas no dicen qué se
perdieron. El protocolo puede pasar sin nada de eso — pero pasar el protocolo no es lo
mismo que tener un diff auditable.

## Lo abierto

Los tres están en `docs/PENDIENTES_consistencia.md`, con su detalle — no se repite acá:

- **P0** — no idempotente (paso 4): causa verificada y arreglo aplicado, falta re-correr.
- **P0** — "Estado" no coincide con "Aplicar" (paso 5): debería caerse con lo anterior, se
  verifica, no se asume.
- **P1** — `BASES.fila_encabezado` con formato de fecha encima; falta saber si `getValues()`
  devuelve número o `Date`. Se une a H-2.

---

## Instrucciones de retoma — los pasos exactos

Nombres de menú tal como figuran **hoy** (el menú se reescribió a mitad de sesión: ahora se
declara por tabla `MENU_` en `Codigo.gs`). Menú raíz: **▶ Motor de Informes**.

1. **Vaciar a mano las dos hojas de reporte**: `DIFF_CONFIGURACION` y
   `ESTADO_CONFIGURACION`. Siguen sin marca de corrida (es **C.2-2**, abierto), así que el
   output viejo no se distingue del nuevo. Sin esto, lo que se lea después es ambiguo.
2. **Diagnóstico → Fechas y mapeo → "Tipos de fechas de ventana (solo lectura)"**. No toca
   nada. Ahora releva también `BASES.fila_encabezado`: contesta si esa columna devuelve
   número o `Date`, que es lo que decide si el P1 es "sacarle el formato" o "hay un bug en
   `Union.gs:36`/`:261`".
3. **Configuración → "Aplicar configuración"**.
4. **Configuración → "Aplicar configuración"** otra vez, sin tocar nada en el medio.
   **Criterio: cero líneas de cambio.** Si aparece alguna, anotar cuál — es el paso 4.
5. **Configuración → "Estado de configuración"**. Criterio: cero discrepancias, y que
   coincida con lo que acaba de reportar el apply — es el paso 5.

Si los tres pasan, recién ahí se commitea `Instalar.gs` + `Fechas.gs` + las dos entradas de
documentación, como una sola unidad, y se completa el `commit <pendiente>` de la bitácora.
